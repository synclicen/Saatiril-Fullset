     1→     1→'use client'
     2→     2→
     3→     3→import {
     4→     4→  useCallback,
     5→     5→  useEffect,
     6→     6→  useMemo,
     7→     7→  useRef,
     8→     8→  useState,
     9→     9→} from 'react'
    10→    10→import { Card, CardContent } from '@/components/ui/card'
    11→    11→import { Button } from '@/components/ui/button'
    12→    12→import { Badge } from '@/components/ui/badge'
    13→    13→import { ScrollArea } from '@/components/ui/scroll-area'
    14→    14→import { Input } from '@/components/ui/input'
    15→    15→import {
    16→    16→  Select,
    17→    17→  SelectContent,
    18→    18→  SelectItem,
    19→    19→  SelectTrigger,
    20→    20→  SelectValue,
    21→    21→} from '@/components/ui/select'
    22→    22→import {
    23→    23→  Camera,
    24→    24→  CheckCircle2,
    25→    25→  Clock,
    26→    26→  Loader2,
    27→    27→  Monitor,
    28→    28→  Search,
    29→    29→  User,
    30→    30→  Video,
    31→    31→  VideoOff,
    32→    32→  Aperture,
    33→    33→  Frame,
    34→    34→  List,
    35→    35→  X,
    36→    36→  SwitchCamera,
    37→    37→  Brain,
    38→    38→  Sparkles,
    39→    39→  Zap,
    40→    40→  Timer,
    41→    41→  Hand,
    42→    42→  FolderOpen,
    43→    43→  HardDrive,
    44→    44→  CheckCircle,
    45→    45→  AlertTriangle,
    46→    46→} from 'lucide-react'
    47→    47→import {
    48→    48→  useSaatirilStore,
    49→    49→  type Student,
    50→    50→  type StudentStatus,
    51→    51→  type PhotoHistoryItem,
    52→    52→  type CameraMode,
    53→    53→  mergeDatabases,
    54→    54→  stripFrameForSync,
    55→    55→  preserveFrameOnSync,
    56→    56→  preservePhotoHistoryOnSync,
    57→    57→  mergeCaptureVersions,
    58→    58→  isPhotoshootMode,
    59→    59→  isDualPhotoshootMode,
    60→    60→} from '@/store/use-saatiril-store'
    61→    61→import { emitLocal, onLocal, offLocal } from '@/lib/socket'
    62→    62→import { useIsMobile } from '@/hooks/use-mobile'
    63→    63→import { NetworkQualityBadge } from '@/components/saatiril/network-quality-badge'
    64→    64→import { useAIDetection, type AIMomentEvent } from '@/hooks/use-ai-detection'
    65→    65→import { usePalmDetection } from '@/hooks/use-palm-detection'
    66→    66→import { useToast } from '@/hooks/use-toast'
    67→    67→import {
    68→    68→  isElectronSaveAvailable,
    69→    69→  isBrowserSaveSupported,
    70→    70→  savePhotoInBrowser,
    71→    71→  downloadPhotoFallback,
    72→    72→  requestBrowserSaveDirectory,
    73→    73→  getBrowserSaveDirectory,
    74→    74→  hasBrowserSaveDirectory,
    75→    75→  browserSaveStorageKey,
    76→    76→} from '@/lib/browser-photo-save'
    77→    77→
    78→    78→// ─── Theme tokens ───────────────────────────────────────────────────────────
    79→    79→const THEME = {
    80→    80→  bg: '#1a0b2e',
    81→    81→  panel: '#2a164a',
    82→    82→  card: '#3b2263',
    83→    83→  border: '#533485',
    84→    84→  gold: '#d4af37',
    85→    85→  muted: '#c4b5fd',
    86→    86→} as const
    87→    87→
    88→    88→// ─── Shutter mode types ────────────────────────────────────────────────────
    89→    89→type ShutterMode = 'manual' | 'timer-3' | 'timer-5' | 'timer-10' | 'ai'
    90→    90→
    91→    91→const SHUTTER_MODES: { id: ShutterMode; label: string; shortLabel: string; icon: React.ReactNode; modesAllowed?: CameraMode[] }[] = [
    92→    92→  { id: 'manual', label: 'Manual', shortLabel: 'Manual', icon: <Camera className="size-3" /> },
    93→    93→  { id: 'timer-3', label: 'Timer 3 detik', shortLabel: '3s', icon: <Timer className="size-3" /> },
    94→    94→  { id: 'timer-5', label: 'Timer 5 detik', shortLabel: '5s', icon: <Timer className="size-3" /> },
    95→    95→  { id: 'timer-10', label: 'Timer 10 detik', shortLabel: '10s', icon: <Timer className="size-3" /> },
    96→    96→  { id: 'ai', label: 'AI Pintar', shortLabel: 'AI', icon: <Brain className="size-3" />, modesAllowed: ['single', 'dual'] },
    97→    97→]
    98→    98→
    99→    99→function getTimerDuration(mode: ShutterMode): number {
   100→   100→  switch (mode) {
   101→   101→    case 'timer-3': return 3
   102→   102→    case 'timer-5': return 5
   103→   103→    case 'timer-10': return 10
   104→   104→    default: return 0
   105→   105→  }
   106→   106→}
   107→   107→
   108→   108→function isTimerMode(mode: ShutterMode): boolean {
   109→   109→  return mode === 'timer-3' || mode === 'timer-5' || mode === 'timer-10'
   110→   110→}
   111→   111→
   112→   112→// ─── Filter preset map ──────────────────────────────────────────────────────
   113→   113→const PRESET_FILTERS: Record<string, string> = {
   114→   114→  original: 'none',
   115→   115→  studio: 'brightness(1.1) contrast(1.05) saturate(1.1)',
   116→   116→  cinematic: 'sepia(0.15) contrast(1.1) brightness(0.95) saturate(1.3)',
   117→   117→  pro: 'contrast(1.25) brightness(1.05) saturate(1.15)',
   118→   118→  vivid: 'brightness(1.08) contrast(1.12) saturate(1.45) hue-rotate(5deg)',
   119→   119→  softPortrait: 'brightness(1.12) contrast(0.92) saturate(1.08) sepia(0.08)',
   120→   120→  classicFilm: 'brightness(1.02) contrast(1.15) saturate(0.85) sepia(0.2)',
   121→   121→  dramaticBW: 'brightness(1.05) contrast(1.35) saturate(0) grayscale(1)',
   122→   122→  warmSunset: 'brightness(1.06) contrast(1.08) saturate(1.3) sepia(0.18) hue-rotate(-10deg)',
   123→   123→}
   124→   124→
   125→   125→// ─── Ratio parser ───────────────────────────────────────────────────────────
   126→   126→function parseRatio(ratioStr: string): number {
   127→   127→  const parts = ratioStr.split(':')
   128→   128→  if (parts.length === 2) {
   129→   129→    const w = parseFloat(parts[0])
   130→   130→    const h = parseFloat(parts[1])
   131→   131→    if (w > 0 && h > 0) return w / h
   132→   132→  }
   133→   133→  return 4 / 3
   134→   134→}
   135→   135→
   136→   136→// ─── Helpers ────────────────────────────────────────────────────────────────
   137→   137→// DEFENSIVE: nama/nim can be undefined if a photoHistory entry got corrupted
   138→   138→// during reset+retake cycles. Never let .trim() crash the whole app.
   139→   139→function sanitizeNama(nama: string | undefined | null): string {
   140→   140→  return (nama ?? '').trim().replace(/\s+/g, '_').replace(/[^a-zA-Z0-9_]/g, '')
   141→   141→}
   142→   142→
   143→   143→function sanitizeNim(nim: string | undefined | null): string {
   144→   144→  return (nim ?? '').toString().trim().replace(/[^a-zA-Z0-9_-]/g, '')
   145→   145→}
   146→   146→
   147→   147→/**
   148→   148→ * Build a versioned filename for standard mode (Toga + Ijazah).
   149→   149→ * version: 1 = first capture, 2+ = retake after MC reset.
   150→   150→ * v1 → `NIM_Nama_1_Toga.jpg` (no version suffix for backwards compat)
   151→   151→ * v2 → `NIM_Nama_1_Toga_v2.jpg`, v3 → `..._v3.jpg`, …
   152→   152→ */
   153→   153→function buildFilename(nim: string | undefined, nama: string | undefined, suffix: number, type: string, version: number = 1): string {
   154→   154→  const base = `${sanitizeNim(nim)}_${sanitizeNama(nama)}_${suffix}_${type}`
   155→   155→  return version > 1 ? `${base}_v${version}.jpg` : `${base}.jpg`
   156→   156→}
   157→   157→
   158→   158→/**
   159→   159→ * Build a versioned filename for photoshoot mode.
   160→   160→ * version: 1 = first capture, 2+ = retake after MC reset.
   161→   161→ * v1 → `NIM_Nama.jpg` or `NIM_Nama_Ch2.jpg` (no version suffix)
   162→   162→ * v2 → `NIM_Nama_v2.jpg` or `NIM_Nama_Ch2_v2.jpg`, …
   163→   163→ */
   164→   164→function buildPhotoshootFilename(nim: string | undefined, nama: string | undefined, channel: number, version: number = 1): string {
   165→   165→  const base = `${sanitizeNim(nim)}_${sanitizeNama(nama)}`
   166→   166→  const withCh = channel > 1 ? `${base}_Ch${channel}` : base
   167→   167→  return version > 1 ? `${withCh}_v${version}.jpg` : `${withCh}.jpg`
   168→   168→}
   169→   169→
   170→   170→/**
   171→   171→ * Storage key for the per-project browser save directory handle.
   172→   172→ * In browser mode (no Electron), the operator picks a folder once via the
   173→   173→ * File System Access API; the handle is cached in IndexedDB under this key.
   174→   174→ */
   175→   175→function photoSaveStorageKey(projectId: string | undefined): string {
   176→   176→  return projectId ? browserSaveStorageKey(projectId) : 'saatiril_default'
   177→   177→}
   178→   178→
   179→   179→function isActiveStatus(status: StudentStatus): boolean {
   180→   180→  return status.startsWith('active')
   181→   181→}
   182→   182→
   183→   183→function getActiveChannel(status: StudentStatus): number | null {
   184→   184→  if (!isActiveStatus(status)) return null
   185→   185→  const ch = status.split('_')[1]
   186→   186→  return ch ? parseInt(ch, 10) : null
   187→   187→}
   188→   188→
   189→   189→function statusLabel(status: StudentStatus): string {
   190→   190→  if (status === 'pending') return 'Menunggu'
   191→   191→  if (status === 'sent') return 'Dikirim'
   192→   192→  if (status === 'done') return 'Selesai'
   193→   193→  const ch = getActiveChannel(status)
   194→   194→  return ch != null ? `Foto Ch.${ch}` : 'Aktif'
   195→   195→}
   196→   196→
   197→   197→function fitAspectRatio(
   198→   198→  availW: number,
   199→   199→  availH: number,
   200→   200→  ratio: number,
   201→   201→): { width: number; height: number } {
   202→   202→  const hFromW = availW / ratio
   203→   203→  if (hFromW <= availH) {
   204→   204→    return { width: availW, height: hFromW }
   205→   205→  }
   206→   206→  const wFromH = availH * ratio
   207→   207→  return { width: wFromH, height: availH }
   208→   208→}
   209→   209→
   210→   210→// ─── Capture state machine ──────────────────────────────────────────────────
   211→   211→type CapturePhase = 'standby' | 'ready-1' | 'ready-2' | 'sending'
   212→   212→
   213→   213→// ─── Video device info ──────────────────────────────────────────────────────
   214→   214→interface VideoDeviceInfo {
   215→   215→  deviceId: string
   216→   216→  label: string
   217→   217→}
   218→   218→
   219→   219→// ─── Socket event data shapes ───────────────────────────────────────────────
   220→   220→interface McCallData {
   221→   221→  student: Student
   222→   222→  channel: number
   223→   223→}
   224→   224→
   225→   225→interface SyncDbData {
   226→   226→  project: {
   227→   227→    id: string
   228→   228→    name: string
   229→   229→    config: {
   230→   230→      mode: CameraMode
   231→   231→      ratio: string
   232→   232→      preset: string
   233→   233→      targetFolder: string
   234→   234→      frame: string | null
   235→   235→    }
   236→   236→    database: Student[]
   237→   237→    photoHistory: PhotoHistoryItem[]
   238→   238→  }
   239→   239→}
   240→   240→
   241→   241→// ─── Component ──────────────────────────────────────────────────────────────
   242→   242→export function OperatorPanel({ readOnly = false }: { readOnly?: boolean }) {
   243→   243→  const isMobile = useIsMobile()
   244→   244→  const { toast } = useToast()
   245→   245→
   246→   246→  // ── Store ────────────────────────────────────────────────────────────────
   247→   247→  const currentProject = useSaatirilStore((s) => s.currentProject)
   248→   248→  const myChannel = useSaatirilStore((s) => s.myChannel)
   249→   249→  const opCurrentTarget = useSaatirilStore((s) => s.opCurrentTarget)
   250→   250→  const opCapturedPhotos = useSaatirilStore((s) => s.opCapturedPhotos)
   251→   251→  const setOpCurrentTarget = useSaatirilStore((s) => s.setOpCurrentTarget)
   252→   252→  const addOpCapturedPhoto = useSaatirilStore((s) => s.addOpCapturedPhoto)
   253→   253→  const resetOpState = useSaatirilStore((s) => s.resetOpState)
   254→   254→  const updateStudentStatus = useSaatirilStore((s) => s.updateStudentStatus)
   255→   255→  const updateCurrentProject = useSaatirilStore((s) => s.updateCurrentProject)
   256→   256→  const saveProjectsToStorageNow = useSaatirilStore((s) => s.saveProjectsToStorageNow)
   257→   257→
   258→   258→  // ── Local state ──────────────────────────────────────────────────────────
   259→   259→  const [videoDevices, setVideoDevices] = useState<VideoDeviceInfo[]>([])
   260→   260→  const [selectedDeviceId, setSelectedDeviceId] = useState<string>('')
   261→   261→  const [cameraAvailable, setCameraAvailable] = useState(false)
   262→   262→  const [flashVisible, setFlashVisible] = useState(false)
   263→   263→  const [sending, setSending] = useState(false)
   264→   264→  const [cameraDims, setCameraDims] = useState({ width: 0, height: 0 })
   265→   265→  const [showQueueOnMobile, setShowQueueOnMobile] = useState(false)
   266→   266→  const [opSearchQuery, setOpSearchQuery] = useState('')
   267→   267→  // Buffer for MC_CALL events that arrive before the database updates via SYNC_DB
   268→   268→  const [mcCallBuffer, setMcCallBuffer] = useState<Student[]>([])
   269→   269→  const isCapturingRef = useRef(false)
   270→   270→  // Browser-mode save folder status: whether the operator has picked a Chrome
   271→   271→  // folder via the File System Access API. null = checking, true = ready,
   272→   272→  // false = not picked (photos will prompt to pick).
   273→   273→  const [rawBrowserFolderReady, setRawBrowserFolderReady] = useState<boolean | null>(null)
   274→   274→
   275→   275→  // ── Shutter mode state ───────────────────────────────────────────────────
   276→   276→  const [shutterMode, setShutterMode] = useState<ShutterMode>('manual')
   277→   277→  // Palm trigger: when ON, showing an open palm to the camera triggers the
   278→   278→  // selected shutter mode (manual → instant photo, timer → starts countdown).
   279→   279→  // This is a hands-free trigger, NOT a shutter mode itself.
   280→   280→  const [palmTriggerEnabled, setPalmTriggerEnabled] = useState(false)
   281→   281→  const [timerCountdown, setTimerCountdown] = useState<number>(0)
   282→   282→  const timerIntervalRef = useRef<ReturnType<typeof setInterval> | null>(null)
   283→   283→  const timerActiveRef = useRef(false)
   284→   284→
   285→   285→  // ── AI auto-capture ──────────────────────────────────────────────────────
   286→   286→  const ai = useAIDetection()
   287→   287→
   288→   288→  // ── Palm detection (selfie-style shutter) ────────────────────────────────
   289→   289→  const palm = usePalmDetection()
   290→   290→
   291→   291→  // ── Refs ─────────────────────────────────────────────────────────────────
   292→   292→  const videoRef = useRef<HTMLVideoElement>(null)
   293→   293→  const canvasRef = useRef<HTMLCanvasElement>(null)
   294→   294→  const flashRef = useRef<HTMLDivElement>(null)
   295→   295→  const activeRowRef = useRef<HTMLDivElement>(null)
   296→   296→  const nextRowRef = useRef<HTMLDivElement>(null)
   297→   297→  const streamRef = useRef<MediaStream | null>(null)
   298→   298→  const selectedDeviceRef = useRef<string>('')
   299→   299→  const frameImgRef = useRef<HTMLImageElement | null>(null)
   300→   300→  const cameraZoneRef = useRef<HTMLDivElement>(null)
   301→   301→
   302→   302→  // ── Derived config ───────────────────────────────────────────────────────
   303→   303→  const config = currentProject?.config
   304→   304→  const aspectRatio = config?.ratio ? parseRatio(config.ratio) : 4 / 3
   305→   305→  const cssFilter = config?.preset ? PRESET_FILTERS[config.preset] ?? 'none' : 'none'
   306→   306→  const frameData = (config?.frame && config.frame !== '__FRAME_SAVED__') ? config.frame : null
   307→   307→
   308→   308→  // ── Resize Observer: calculate camera dimensions ─────────────────────────
   309→   309→  useEffect(() => {
   310→   310→    const zone = cameraZoneRef.current
   311→   311→    if (!zone) return
   312→   312→
   313→   313→    const updateSize = () => {
   314→   314→      const rect = zone.getBoundingClientRect()
   315→   315→      const padding = isMobile ? 8 : 16
   316→   316→      const availW = rect.width - padding
   317→   317→      const availH = rect.height - padding
   318→   318→      if (availW > 0 && availH > 0) {
   319→   319→        setCameraDims(fitAspectRatio(availW, availH, aspectRatio))
   320→   320→      }
   321→   321→    }
   322→   322→
   323→   323→    updateSize()
   324→   324→    const observer = new ResizeObserver(updateSize)
   325→   325→    observer.observe(zone)
   326→   326→    return () => observer.disconnect()
   327→   327→  }, [aspectRatio, isMobile])
   328→   328→
   329→   329→  // ── Preload frame image ──────────────────────────────────────────────────
   330→   330→  useEffect(() => {
   331→   331→    if (frameData) {
   332→   332→      const img = new Image()
   333→   333→      img.crossOrigin = 'anonymous'
   334→   334→      img.onload = () => { frameImgRef.current = img }
   335→   335→      img.onerror = () => { frameImgRef.current = null }
   336→   336→      img.src = frameData
   337→   337→    } else {
   338→   338→      frameImgRef.current = null
   339→   339→    }
   340→   340→  }, [frameData])
   341→   341→
   342→   342→  // ── Derived data ─────────────────────────────────────────────────────────
   343→   343→  const mode = currentProject?.config.mode ?? 'single'
   344→   344→  const photoshoot = isPhotoshootMode(mode)
   345→   345→  const dualPhotoshoot = isDualPhotoshootMode(mode)
   346→   346→
   347→   347→  // AI mode is only allowed in single/dual mode
   348→   348→  const aiAllowed = mode === 'single' || mode === 'dual'
   349→   349→
   350→   350→  // Compute effective shutter mode — if AI is selected but not allowed, fallback to manual
   351→   351→  const effectiveShutterMode: ShutterMode = (shutterMode === 'ai' && !aiAllowed) ? 'manual' : shutterMode
   352→   352→
   353→   353→  const channelStudents = useMemo<Student[]>(() => {
   354→   354→    if (!currentProject) return []
   355→   355→    if (photoshoot) {
   356→   356→      // In photoshoot modes, show all students
   357→   357→      return currentProject.database
   358→   358→    }
   359→   359→    return currentProject.database.filter((s) => s.assignedChannel === myChannel)
   360→   360→  }, [currentProject, myChannel, photoshoot])
   361→   361→
   362→   362→  // ── Operator queue: derived from database + MC_CALL buffer ──────────────────
   363→   363→  const opQueue = useMemo<Student[]>(() => {
   364→   364→    if (!photoshoot || !currentProject) return []
   365→   365→    const alreadyPhotographed = new Set(
   366→   366→      currentProject.photoHistory
   367→   367→        .filter((h) => h.channel === myChannel)
   368→   368→        .map((h) => h.student.id)
   369→   369→    )
   370→   370→    const doneIds = new Set(
   371→   371→      currentProject.database.filter((s) => s.status === 'done').map((s) => s.id)
   372→   372→    )
   373→   373→    const dbQueueIds = new Set<string>()
   374→   374→    const dbItems = currentProject.database.filter(
   375→   375→      (s) => s.status === 'sent' && !alreadyPhotographed.has(s.id)
   376→   376→    )
   377→   377→    dbItems.forEach((s) => dbQueueIds.add(s.id))
   378→   378→    const bufferItems = mcCallBuffer.filter(
   379→   379→      (s) => !dbQueueIds.has(s.id) && !doneIds.has(s.id) && !alreadyPhotographed.has(s.id)
   380→   380→    )
   381→   381→    return [...dbItems, ...bufferItems]
   382→   382→  }, [photoshoot, currentProject, myChannel, mcCallBuffer])
   383→   383→
   384→   384→  const opSearchResults = useMemo<Student[]>(() => {
   385→   385→    if (!opSearchQuery.trim()) return opQueue
   386→   386→    const q = opSearchQuery.toLowerCase().trim()
   387→   387→    return opQueue.filter(
   388→   388→      (s) => s.nim.toLowerCase().includes(q) || s.nama.toLowerCase().includes(q)
   389→   389→    )
   390→   390→  }, [opSearchQuery, opQueue])
   391→   391→
   392→   392→  const currentlyActive = useMemo<Student | null>(() => {
   393→   393→    if (photoshoot) {
   394→   394→      return opCurrentTarget
   395→   395→    }
   396→   396→    const targetStatus: StudentStatus = `active_${myChannel}`
   397→   397→    return channelStudents.find((s) => s.status === targetStatus) ?? null
   398→   398→  }, [channelStudents, myChannel, photoshoot, opCurrentTarget])
   399→   399→
   400→   400→  const nextPending = useMemo<Student | null>(() => {
   401→   401→    return channelStudents.find((s) => s.status === 'pending') ?? null
   402→   402→  }, [channelStudents])
   403→   403→
   404→   404→  const remainingCount = useMemo<number>(() => {
   405→   405→    return channelStudents.filter((s) => s.status === 'pending').length
   406→   406→  }, [channelStudents])
   407→   407→
   408→   408→  const hasActiveTarget = opCurrentTarget !== null && !!opCurrentTarget.id && (opCurrentTarget.nama !== undefined || opCurrentTarget.nim !== undefined)
   409→   409→
   410→   410→  const capturePhase = useMemo<CapturePhase>(() => {
   411→   411→    if (sending) return 'sending'
   412→   412→    if (!hasActiveTarget) return 'standby'
   413→   413→    if (photoshoot) {
   414→   414→      if (opCapturedPhotos.length === 0) return 'ready-1'
   415→   415→      return 'sending'
   416→   416→    }
   417→   417→    if (opCapturedPhotos.length === 0) return 'ready-1'
   418→   418→    if (opCapturedPhotos.length === 1) return 'ready-2'
   419→   419→    return 'standby'
   420→   420→  }, [sending, hasActiveTarget, opCapturedPhotos.length, photoshoot])
   421→   421→
   422→   422→  // ── Auto-scroll refs ─────────────────────────────────────────────────────
   423→   423→  useEffect(() => {
   424→   424→    const target = activeRowRef.current ?? nextRowRef.current
   425→   425→    if (target) target.scrollIntoView({ behavior: 'smooth', block: 'center' })
   426→   426→  }, [currentlyActive, nextPending])
   427→   427→
   428→   428→  // ── Camera: enumerate devices ────────────────────────────────────────────
   429→   429→  const enumerateVideoDevices = useCallback(async () => {
   430→   430→    if (typeof navigator === 'undefined' || !navigator.mediaDevices) return
   431→   431→    try {
   432→   432→      const devices = await navigator.mediaDevices.enumerateDevices()
   433→   433→      const videoInputs = devices
   434→   434→        .filter((d) => d.kind === 'videoinput')
   435→   435→        .map((d) => ({
   436→   436→          deviceId: d.deviceId,
   437→   437→          label: d.label || `Kamera ${d.deviceId.slice(0, 6)}`,
   438→   438→        }))
   439→   439→      setVideoDevices(videoInputs)
   440→   440→      if (videoInputs.length > 0 && !selectedDeviceRef.current) {
   441→   441→        setSelectedDeviceId(videoInputs[0].deviceId)
   442→   442→        selectedDeviceRef.current = videoInputs[0].deviceId
   443→   443→      }
   444→   444→    } catch (err) {
   445→   445→      console.error('[SAATIRIL OP] Failed to enumerate devices:', err)
   446→   446→    }
   447→   447→  }, [])
   448→   448→
   449→   449→  // ── Camera: start stream ─────────────────────────────────────────────────
   450→   450→  const startCamera = useCallback(
   451→   451→    async (deviceId?: string) => {
   452→   452→      if (typeof navigator === 'undefined' || !navigator.mediaDevices) {
   453→   453→        setCameraAvailable(false)
   454→   454→        return
   455→   455→      }
   456→   456→      if (streamRef.current) {
   457→   457→        streamRef.current.getTracks().forEach((t) => t.stop())
   458→   458→        streamRef.current = null
   459→   459→      }
   460→   460→
   461→   461→      let constraints: MediaStreamConstraints
   462→   462→      if (deviceId) {
   463→   463→        constraints = {
   464→   464→          video: { deviceId: { exact: deviceId }, width: { ideal: 1920 }, height: { ideal: 1080 } },
   465→   465→          audio: false,
   466→   466→        }
   467→   467→      } else if (isMobile) {
   468→   468→        constraints = {
   469→   469→          video: { facingMode: 'environment', width: { ideal: 1920 }, height: { ideal: 1080 } },
   470→   470→          audio: false,
   471→   471→        }
   472→   472→      } else {
   473→   473→        constraints = {
   474→   474→          video: { width: { ideal: 1920 }, height: { ideal: 1080 } },
   475→   475→          audio: false,
   476→   476→        }
   477→   477→      }
   478→   478→
   479→   479→      try {
   480→   480→        const stream = await navigator.mediaDevices.getUserMedia(constraints)
   481→   481→        streamRef.current = stream
   482→   482→        setCameraAvailable(true)
   483→   483→        if (videoRef.current) videoRef.current.srcObject = stream
   484→   484→        await enumerateVideoDevices()
   485→   485→      } catch (err) {
   486→   486→        console.error('[SAATIRIL OP] Camera access failed:', err)
   487→   487→        if (isMobile && !deviceId) {
   488→   488→          try {
   489→   489→            const fallbackConstraints: MediaStreamConstraints = {
   490→   490→              video: { width: { ideal: 1920 }, height: { ideal: 1080 } },
   491→   491→              audio: false,
   492→   492→            }
   493→   493→            const stream = await navigator.mediaDevices.getUserMedia(fallbackConstraints)
   494→   494→            streamRef.current = stream
   495→   495→            setCameraAvailable(true)
   496→   496→            if (videoRef.current) videoRef.current.srcObject = stream
   497→   497→            await enumerateVideoDevices()
   498→   498→          } catch (fallbackErr) {
   499→   499→            console.error('[SAATIRIL OP] Camera fallback also failed:', fallbackErr)
   500→   500→            setCameraAvailable(false)
   501→   501→          }
   502→   502→        } else {
   503→   503→          setCameraAvailable(false)
   504→   504→        }
   505→   505→      }
   506→   506→    },
   507→   507→    [enumerateVideoDevices, isMobile],
   508→   508→  )
   509→   509→
   510→   510→  useEffect(() => {
   511→   511→    queueMicrotask(() => void startCamera())
   512→   512→    return () => {
   513→   513→      if (streamRef.current) {
   514→   514→        streamRef.current.getTracks().forEach((t) => t.stop())
   515→   515→        streamRef.current = null
   516→   516→      }
   517→   517→    }
   518→   518→  }, [startCamera])
   519→   519→
   520→   520→  useEffect(() => {
   521→   521→    if (selectedDeviceId && selectedDeviceRef.current !== selectedDeviceId) {
   522→   522→      selectedDeviceRef.current = selectedDeviceId
   523→   523→      queueMicrotask(() => void startCamera(selectedDeviceId))
   524→   524→    }
   525→   525→  }, [selectedDeviceId, startCamera])
   526→   526→
   527→   527→  useEffect(() => {
   528→   528→    if (typeof navigator === 'undefined' || !navigator.mediaDevices) return
   529→   529→    const handler = () => enumerateVideoDevices()
   530→   530→    navigator.mediaDevices.addEventListener('devicechange', handler)
   531→   531→    return () => { navigator.mediaDevices.removeEventListener('devicechange', handler) }
   532→   532→  }, [enumerateVideoDevices])
   533→   533→
   534→   534→  // ── Switch camera (mobile: toggle front/rear) ────────────────────────────
   535→   535→  const handleSwitchCamera = useCallback(async () => {
   536→   536→    if (videoDevices.length < 2) return
   537→   537→    const currentIdx = videoDevices.findIndex((d) => d.deviceId === selectedDeviceId)
   538→   538→    const nextIdx = (currentIdx + 1) % videoDevices.length
   539→   539→    const nextDeviceId = videoDevices[nextIdx].deviceId
   540→   540→    setSelectedDeviceId(nextDeviceId)
   541→   541→    selectedDeviceRef.current = nextDeviceId
   542→   542→  }, [videoDevices, selectedDeviceId])
   543→   543→
   544→   544→  // ── Browser save folder (File System Access API) ─────────────────────────
   545→   545→  // When the operator opens the app in Chrome (NOT Electron), photos can't
   546→   546→  // use the IPC save path. Instead we let them pick a real folder on disk via
   547→   547→  // the File System Access API — the handle is cached in IndexedDB so they
   548→   548→  // only pick once (and re-grant permission with a single click after reload).
   549→   549→  const inBrowserMode = !isElectronSaveAvailable()
   550→   550→  const fsSupported = isBrowserSaveSupported()
   551→   551→  const browserStorageKey = currentProject?.id ?? 'saatiril_default'
   552→   552→
   553→   553→  // Effective status (computed inline — no setState in effect body):
   554→   554→  // - Not in browser mode → null (UI hides the folder picker entirely)
   555→   555→  // - Browser mode but no File System Access API → false (fallback to download)
   556→   556→  // - Browser mode + FS API → async-checked value from state
   557→   557→  const browserFolderReady: boolean | null = !inBrowserMode
   558→   558→    ? null
   559→   559→    : !fsSupported
   560→   560→      ? false
   561→   561→      : rawBrowserFolderReady
   562→   562→
   563→   563→  // Async check: does a saved directory handle exist + have permission?
   564→   564→  // Only runs in browser mode with FS API support. setState is called only
   565→   565→  // in the async callback (never synchronously in the effect body).
   566→   566→  useEffect(() => {
   567→   567→    if (!inBrowserMode || !fsSupported) return
   568→   568→    let cancelled = false
   569→   569→    getBrowserSaveDirectory(browserStorageKey).then((handle) => {
   570→   570→      if (!cancelled) setRawBrowserFolderReady(!!handle)
   571→   571→    })
   572→   572→    return () => { cancelled = true }
   573→   573→  }, [inBrowserMode, fsSupported, browserStorageKey])
   574→   574→
   575→   575→  const handlePickBrowserFolder = useCallback(async () => {
   576→   576→    const handle = await requestBrowserSaveDirectory(browserStorageKey)
   577→   577→    if (handle) {
   578→   578→      setRawBrowserFolderReady(true)
   579→   579→      toast({
   580→   580→        title: 'Folder Simpan Aktif',
   581→   581→        description: 'Foto akan tersimpan ke folder Chrome yang dipilih.',
   582→   582→      })
   583→   583→    }
   584→   584→  }, [browserStorageKey, toast])
   585→   585→
   586→   586→  // ── Refs for stable handlers ─────────────────────────────────────────────
   587→   587→  const myChannelRef = useRef(myChannel)
   588→   588→  const currentProjectRef = useRef(currentProject)
   589→   589→  useEffect(() => { myChannelRef.current = myChannel }, [myChannel])
   590→   590→  useEffect(() => { currentProjectRef.current = currentProject }, [currentProject])
   591→   591→
   592→   592→  // ── State recovery (non-photoshoot only) ────────────────────────────────
   593→   593→  useEffect(() => {
   594→   594→    if (!currentProject || photoshoot) return
   595→   595→    const activeStudent = currentProject.database.find((s) => {
   596→   596→      return s.assignedChannel === myChannel && isActiveStatus(s.status)
   597→   597→    })
   598→   598→    if (activeStudent) {
   599→   599→      setOpCurrentTarget(activeStudent)
   600→   600→    } else if (opCurrentTarget && !isActiveStatus(opCurrentTarget.status)) {
   601→   601→      setOpCurrentTarget(null)
   602→   602→    }
   603→   603→  }, [currentProject, myChannel, setOpCurrentTarget, photoshoot])
   604→   604→
   605→   605→  // ── Socket: MC_CALL ─────────────────────────────────────────────────────
   606→   606→  useEffect(() => {
   607→   607→    const handleMcCall = (data: McCallData) => {
   608→   608→      if (data.channel !== myChannelRef.current) return
   609→   609→      console.log('[SAATIRIL OP] MC_CALL received:', data.student.nama, 'status:', data.student.status, 'Ch.', data.channel)
   610→   610→      if (photoshoot) {
   611→   611→        // REPLACE any existing buffer entry for this student instead of
   612→   612→        // dedup-skipping. This ensures a RE-SEND (after reset/retake) updates
   613→   613→        // the buffer with fresh data instead of being silently dropped.
   614→   614→        setMcCallBuffer((prev) => {
   615→   615→          const without = prev.filter((s) => s.id !== data.student.id)
   616→   616→          return [...without, data.student]
   617→   617→        })
   618→   618→      } else {
   619→   619→        setOpCurrentTarget(data.student)
   620→   620→      }
   621→   621→    }
   622→   622→    onLocal('MC_CALL', handleMcCall)
   623→   623→    return () => { offLocal('MC_CALL', handleMcCall) }
   624→   624→  }, [setOpCurrentTarget, photoshoot])
   625→   625→
   626→   626→  // ── Socket: STUDENT_RESET — explicit reset/retake signal from MC ────────
   627→   627→  // This bypasses the normal SYNC_DB merge (which blocks status regression:
   628→   628→  // pending priority 0 < sent/done) so the operator can fully clear a student
   629→   629→  // for retake: drop from mcCallBuffer, clear active target, remove the
   630→   630→  // photoHistory entry on this channel, and set status to 'pending' locally.
   631→   631→  useEffect(() => {
   632→   632→    const handleStudentReset = (data: { studentId: string; channel: number }) => {
   633→   633→      if (data.channel !== myChannelRef.current) return
   634→   634→      console.log('[SAATIRIL OP] STUDENT_RESET received — clearing for retake:', data.studentId, 'Ch.', data.channel)
   635→   635→
   636→   636→      // 1. Drop from mcCallBuffer
   637→   637→      setMcCallBuffer((prev) => prev.filter((s) => s.id !== data.studentId))
   638→   638→
   639→   639→      // 2. Clear active target if it matches
   640→   640→      setOpCurrentTarget((cur) => (cur?.id === data.studentId ? null : cur))
   641→   641→
   642→   642→      // 3. Remove this channel's photoHistory entry + reset status locally
   643→   643→      //    (bypasses mergeDatabases priority which would otherwise ignore the
   644→   644→      //    'pending' regression coming via SYNC_DB).
   645→   645→      //    IMPORTANT: read via useSaatirilStore.getState() (synchronous, latest)
   646→   646→      //    instead of currentProjectRef.current (stale ref updated only after
   647→   647→      //    render). This prevents a race where SYNC_DB arrives right after
   648→   648→      //    STUDENT_RESET and sees the OLD photoHistory — which would re-add
   649→   649→      //    the cleared entry via preservePhotoHistoryOnSync.
   650→   650→      const proj = useSaatirilStore.getState().currentProject
   651→   651→      if (proj) {
   652→   652→        const cleanedHistory = proj.photoHistory.filter(
   653→   653→          (h) => !(h.student.id === data.studentId && h.channel === myChannelRef.current),
   654→   654→        )
   655→   655→        const cleanedDb = proj.database.map((s) =>
   656→   656→          s.id === data.studentId ? { ...s, status: 'pending' as StudentStatus } : s,
   657→   657→        )
   658→   658→        updateCurrentProject({ ...proj, database: cleanedDb, photoHistory: cleanedHistory })
   659→   659→      }
   660→   660→    }
   661→   661→    onLocal('STUDENT_RESET', handleStudentReset)
   662→   662→    return () => { offLocal('STUDENT_RESET', handleStudentReset) }
   663→   663→  }, [setOpCurrentTarget, updateCurrentProject])
   664→   664→
   665→   665→  // ── Socket: SYNC_DB ─────────────────────────────────────────────────────
   666→   666→  useEffect(() => {
   667→   667→    const handleSyncDb = (data: SyncDbData) => {
   668→   668→      // Read latest state synchronously (avoids stale currentProjectRef race
   669→   669→      // when SYNC_DB arrives immediately after STUDENT_RESET / other updates).
   670→   670→      const proj = useSaatirilStore.getState().currentProject
   671→   671→      if (!proj) return
   672→   672→      const mergedDb = mergeDatabases(proj.database, data.project.database)
   673→   673→      const mergedConfig = preserveFrameOnSync(data.project.config, proj.config)
   674→   674→      const mergedPhotoHistory = preservePhotoHistoryOnSync(
   675→   675→        data.project.photoHistory ?? [],
   676→   676→        proj.photoHistory,
   677→   677→      )
   678→   678→      // Merge captureVersions (MAX per key) so retake version numbers never
   679→   679→      // regress across clients. The operator who just captured has the
   680→   680→      // highest version; other clients adopt it via this merge.
   681→   681→      const mergedVersions = mergeCaptureVersions(
   682→   682→        proj.captureVersions,
   683→   683→        (data.project as any).captureVersions,
   684→   684→      )
   685→   685→      updateCurrentProject({ ...proj, database: mergedDb, photoHistory: mergedPhotoHistory, config: mergedConfig, captureVersions: mergedVersions })
   686→   686→
   687→   687→      if (!isPhotoshootMode(data.project.config.mode)) {
   688→   688→        const ch = myChannelRef.current
   689→   689→        const activeStudent = data.project.database.find((s: Student) => {
   690→   690→          return s.assignedChannel === ch && isActiveStatus(s.status)
   691→   691→        })
   692→   692→        if (activeStudent) setOpCurrentTarget(activeStudent)
   693→   693→      }
   694→   694→
   695→   695→      const doneIds = new Set(
   696→   696→        data.project.database.filter((s: Student) => s.status === 'done').map((s: Student) => s.id)
   697→   697→      )
   698→   698→      if (doneIds.size > 0) {
   699→   699→        setMcCallBuffer((prev) => prev.filter((s) => !doneIds.has(s.id)))
   700→   700→      }
   701→   701→
   702→   702→      // Dual-photoshoot: when EITHER camera takes a photo, the participant is
   703→   703→      // considered done. If our current target is now 'done' (because the OTHER
   704→   704→      // operator took the photo), clear our target + captured photos so we don't
   705→   705→      // take a redundant photo. This realizes the "1 camera is enough" rule.
   706→   706→      const curTarget = useSaatirilStore.getState().opCurrentTarget
   707→   707→      if (curTarget && doneIds.has(curTarget.id)) {
   708→   708→        console.log('[SAATIRIL OP] SYNC_DB: current target is now done — clearing (other operator finished):', curTarget.nama)
   709→   709→        resetOpState()
   710→   710→      }
   711→   711→    }
   712→   712→    onLocal('SYNC_DB', handleSyncDb)
   713→   713→    return () => { offLocal('SYNC_DB', handleSyncDb) }
   714→   714→  }, [setOpCurrentTarget, resetOpState, updateCurrentProject])
   715→   715→
   716→   716→  // ── Finalize capture ────────────────────────────────────────────────────
   717→   717→  // OPTIMIZED: Removed 400ms of unnecessary delays, added STUDENT_DONE lightweight
   718→   718→  // event for immediate MC unblocking, and SYNC_DB now strips photos (tiny payload).
   719→   719→  const finalizeCapture = useCallback(
   720→   720→    (canvas: HTMLCanvasElement) => {
   721→   721→      setFlashVisible(true)
   722→   722→      setTimeout(() => setFlashVisible(false), 200)
   723→   723→
   724→   724→      const dataUrl = canvas.toDataURL('image/jpeg', 0.95)
   725→   725→      addOpCapturedPhoto(dataUrl)
   726→   726→
   727→   727→      const currentPhotos = useSaatirilStore.getState().opCapturedPhotos
   728→   728→      const currentTarget = useSaatirilStore.getState().opCurrentTarget
   729→   729→      const photoCount = currentPhotos.length
   730→   730→      const currentMode = useSaatirilStore.getState().currentProject?.config.mode ?? 'single'
   731→   731→      const isPhotoshoot = isPhotoshootMode(currentMode)
   732→   732→
   733→   733→      console.log('[SAATIRIL OP] finalizeCapture: photoCount =', photoCount, 'mode =', currentMode)
   734→   734→
   735→   735→      // Helper: update local photoHistory + captureVersions, emit lightweight SYNC_DB.
   736→   736→      // `newVersions` is the updated captureVersions map (with the just-captured
   737→   737→      // student+channel incremented) so version numbers persist across resets.
   738→   738→      const finishCapture = (
   739→   739→        student: Student,
   740→   740→        historyItem: PhotoHistoryItem,
   741→   741→        newVersions?: Record<string, number>,
   742→   742→      ) => {
   743→   743→        const store = useSaatirilStore.getState()
   744→   744→        if (store.currentProject) {
   745→   745→          const existingIdx = store.currentProject.photoHistory.findIndex(
   746→   746→            (h) => h.student.id === student.id && h.channel === myChannel
   747→   747→          )
   748→   748→          let newHistory: PhotoHistoryItem[]
   749→   749→          if (existingIdx !== -1) {
   750→   750→            newHistory = [...store.currentProject.photoHistory]
   751→   751→            newHistory[existingIdx] = historyItem
   752→   752→          } else {
   753→   753→            newHistory = [...store.currentProject.photoHistory, historyItem]
   754→   754→          }
   755→   755→          const updatedProject = {
   756→   756→            ...store.currentProject,
   757→   757→            photoHistory: newHistory,
   758→   758→            captureVersions: newVersions ?? store.currentProject.captureVersions,
   759→   759→          }
   760→   760→          store.updateCurrentProject(updatedProject)
   761→   761→          // SYNC_DB is now lightweight — photos stripped, only metadata sent
   762→   762→          // (captureVersions IS included so admin/MC see the same version numbers)
   763→   763→          emitLocal('SYNC_DB', { project: stripFrameForSync(updatedProject) })
   764→   764→        }
   765→   765→        setSending(false)
   766→   766→        isCapturingRef.current = false
   767→   767→        resetOpState()
   768→   768→      }
   769→   769→
   770→   770→      // ── Compute versioned filename helper ──────────────────────────────────
   771→   771→      // Reads the per-student+channel capture counter from the project, increments
   772→   772→      // it, and returns { version, newVersions, filename } so the caller can
   773→   773→      // (a) use the filename for the disk save, and (b) pass newVersions to
   774→   774→      // finishCapture so the counter persists.
   775→   775→      const proj0 = useSaatirilStore.getState().currentProject
   776→   776→      const computeVersionedFilename = (
   777→   777→        student: Student,
   778→   778→        isPhotoshootModeFlag: boolean,
   779→   779→      ): { version: number; newVersions: Record<string, number>; filename: string } => {
   780→   780→        const versionKey = `${student.id}_${myChannel}`
   781→   781→        const currentVersions = proj0?.captureVersions ?? {}
   782→   782→        const prevVersion = currentVersions[versionKey] ?? 0
   783→   783→        const version = prevVersion + 1
   784→   784→        const newVersions = { ...currentVersions, [versionKey]: version }
   785→   785→        const filename = isPhotoshootModeFlag
   786→   786→          ? buildPhotoshootFilename(student.nim, student.nama, myChannel, version)
   787→   787→          : buildFilename(student.nim, student.nama, 1, 'Toga', version) // placeholder; standard mode overrides below
   788→   788→        return { version, newVersions, filename }
   789→   789→      }
   790→   790→
   791→   791→      // Photoshoot mode: save after 1 photo
   792→   792→      if (isPhotoshoot && photoCount >= 1) {
   793→   793→        if (!currentTarget) {
   794→   794→          console.warn('[SAATIRIL OP] finalizeCapture: opCurrentTarget is null — aborting save')
   795→   795→          // CRITICAL: tell the operator WHY the photo wasn't saved, so they
   796→   796→          // don't think it was captured successfully. This happens when MC
   797→   797→          // resets a student but hasn't re-sent them yet.
   798→   798→          toast({
   799→   799→            title: 'Foto Tidak Tersimpan',
   800→   800→            description: 'Tidak ada peserta aktif. Minta MC mengirim ulang peserta, lalu foto kembali.',
   801→   801→            variant: 'destructive',
   802→   802→          })
   803→   803→          setSending(false)
   804→   804→          isCapturingRef.current = false
   805→   805→          resetOpState()
   806→   806→          return
   807→   807→        }
   808→   808→        setSending(true)
   809→   809→        const student = currentTarget
   810→   810→        const allPhotos = [...currentPhotos]
   811→   811→        const historyItem: PhotoHistoryItem = {
   812→   812→          student: { ...student },
   813→   813→          photos: allPhotos,
   814→   814→          channel: myChannel,
   815→   815→        }
   816→   816→        saveProjectsToStorageNow()
   817→   817→        setMcCallBuffer((prev) => prev.filter((s) => s.id !== student.id))
   818→   818→
   819→   819→        // ── VERSIONED FILENAME ────────────────────────────────────────────────
   820→   820→        // Each capture (incl. retakes after MC reset) gets a unique version so
   821→   821→        // retakes create NEW files on disk instead of overwriting.
   822→   822→        // v1 → `NIM_Nama.jpg`, v2 → `NIM_Nama_v2.jpg`, …
   823→   823→        const { version, newVersions, filename: vFilename } = computeVersionedFilename(student, true)
   824→   824→        console.log(`[SAATIRIL OP] Photoshoot capture v${version} for ${student.nama}: ${vFilename}`)
   825→   825→
   826→   826→        console.log('[SAATIRIL OP] Emitting PHOTOS_SAVED for student:', student.nama, 'channel:', myChannel, 'version:', version)
   827→   827→
   828→   828→        // PHOTOS_SAVED for Admin/gallery (contains photos for display + version)
   829→   829→        emitLocal('PHOTOS_SAVED', {
   830→   830→          student: { ...student, status: student.status },
   831→   831→          photos: allPhotos,
   832→   832→          channel: myChannel,
   833→   833→          version,
   834→   834→          filename: vFilename,
   835→   835→        })
   836→   836→        emitLocal('OP_PROGRESS', { channel: myChannel, status: 'Selesai — Menunggu target...' })
   837→   837→
   838→   838→        // ── SAVE TO DISK ──────────────────────────────────────────────────────
   839→   839→        // Two paths: Electron (window.saatirilAPI.savePhoto) or Browser
   840→   840→        // (File System Access API → real folder on disk). Browser mode is the
   841→   841→        // common case for operators who open the app in Chrome to enable
   842→   842→        // Chrome flags for camera / MediaPipe performance.
   843→   843→        const projConfig = useSaatirilStore.getState().currentProject?.config
   844→   844→        const projId = useSaatirilStore.getState().currentProject?.id
   845→   845→        if (projConfig) {
   846→   846→          if (isElectronSaveAvailable()) {
   847→   847→            // ── Electron path: IPC → fs.writeFileSync ──
   848→   848→            const api = (window as any).saatirilAPI
   849→   849→            const targetFolder = projConfig.targetFolder
   850→   850→            console.log(`[SAATIRIL OP] Saving photo to disk (Electron): ${targetFolder}/${vFilename}`)
   851→   851→            api.savePhoto({ base64Data: allPhotos[0], filename: vFilename, targetFolder }).then((path: string | null) => {
   852→   852→              if (path) {
   853→   853→                console.log(`[SAATIRIL OP] ✓ Photo saved to disk: → ${path}`)
   854→   854→              } else {
   855→   855→                console.warn(`[SAATIRIL OP] ✗ Photo FAILED to save to disk: ${targetFolder}/${vFilename}`)
   856→   856→                toast({
   857→   857→                  title: 'Gagal Simpan ke Disk',
   858→   858→                  description: `Foto ${vFilename} tidak tersimpan. Cek ruang disk & folder target.`,
   859→   859→                  variant: 'destructive',
   860→   860→                })
   861→   861→              }
   862→   862→            }).catch((err: Error) => {
   863→   863→              console.error('[SAATIRIL OP] Error saving photo to disk:', err)
   864→   864→              toast({
   865→   865→                title: 'Error Simpan Foto',
   866→   866→                description: err.message || 'Terjadi kesalahan saat menyimpan ke disk.',
   867→   867→                variant: 'destructive',
   868→   868→              })
   869→   869→            })
   870→   870→          } else {
   871→   871→            // ── Browser path: File System Access API (Chrome/Edge) ──
   872→   872→            // The operator picks a folder once; the handle is cached in
   873→   873→            // IndexedDB so subsequent saves are silent. If no folder picked
   874→   874→            // yet, we prompt them to pick one.
   875→   875→            const storageKey = photoSaveStorageKey(projId)
   876→   876→            console.log(`[SAATIRIL OP] Saving photo to disk (Browser FS Access): ${vFilename}`)
   877→   877→            savePhotoInBrowser(storageKey, allPhotos[0], vFilename).then((savedName: string | null) => {
   878→   878→              if (savedName) {
   879→   879→                console.log(`[SAATIRIL OP] ✓ Photo saved to browser folder: ${savedName}`)
   880→   880→                toast({
   881→   881→                  title: `Foto Tersimpan (v${version})`,
   882→   882→                  description: `${savedName} — tersimpan ke folder Chrome.`,
   883→   883→                })
   884→   884→              } else {
   885→   885→                // No directory picked yet, or permission denied — prompt user.
   886→   886→                console.warn(`[SAATIRIL OP] Browser folder not available — prompting user to pick one`)
   887→   887→                toast({
   888→   888→                  title: 'Pilih Folder Simpan',
   889→   889→                  description: 'Klik tombol "Pilih Folder" lalu foto ulang untuk menyimpan ke disk Chrome.',
   890→   890→                  variant: 'destructive',
   891→   891→                })
   892→   892→              }
   893→   893→            }).catch((err: Error) => {
   894→   894→              console.error('[SAATIRIL OP] Error saving photo to browser folder:', err)
   895→   895→              // Last-resort: download so the photo is at least not lost
   896→   896→              if (isBrowserSaveSupported()) {
   897→   897→                downloadPhotoFallback(allPhotos[0], vFilename)
   898→   898→              }
   899→   899→              toast({
   900→   900→                title: 'Error Simpan Foto',
   901→   901→                description: err.message || 'Terjadi kesalahan saat menyimpan ke folder Chrome.',
   902→   902→                variant: 'destructive',
   903→   903→              })
   904→   904→            })
   905→   905→          }
   906→   906→        }
   907→   907→
   908→   908→        // IMMEDIATE: update local state + emit lightweight SYNC_DB (no delays!)
   909→   909→        finishCapture(student, historyItem, newVersions)
   910→   910→        return
   911→   911→      }
   912→   912→
   913→   913→      // Standard mode (single/dual): 2 photos
   914→   914→      if (photoCount === 1) {
   915→   915→        isCapturingRef.current = false
   916→   916→        emitLocal('OP_PROGRESS', { channel: myChannel, status: 'Pose 1 OK — Siap Foto 2' })
   917→   917→      } else if (photoCount >= 2) {
   918→   918→        if (!currentTarget) {
   919→   919→          console.warn('[SAATIRIL OP] finalizeCapture: opCurrentTarget is null — aborting save')
   920→   920→          toast({
   921→   921→            title: 'Foto Tidak Tersimpan',
   922→   922→            description: 'Tidak ada peserta aktif. Minta MC mengirim ulang peserta, lalu foto kembali.',
   923→   923→            variant: 'destructive',
   924→   924→          })
   925→   925→          setSending(false)
   926→   926→          isCapturingRef.current = false
   927→   927→          resetOpState()
   928→   928→          return
   929→   929→        }
   930→   930→        setSending(true)
   931→   931→        const student = currentTarget
   932→   932→        const allPhotos = [...currentPhotos]
   933→   933→        const historyItem: PhotoHistoryItem = {
   934→   934→          student: { ...student },
   935→   935→          photos: allPhotos,
   936→   936→          channel: myChannel,
   937→   937→        }
   938→   938→        updateStudentStatus(student.id, 'done')
   939→   939→        saveProjectsToStorageNow()
   940→   940→
   941→   941→        // ── VERSIONED FILENAMES ───────────────────────────────────────────────
   942→   942→        // Standard mode: 2 photos (Toga + Ijazah), both share the same version.
   943→   943→        // v1 → `NIM_Nama_1_Toga.jpg` + `NIM_Nama_2_Ijazah.jpg`
   944→   944→        // v2 → `NIM_Nama_1_Toga_v2.jpg` + `NIM_Nama_2_Ijazah_v2.jpg`
   945→   945→        const { version, newVersions } = computeVersionedFilename(student, false)
   946→   946→        const togaFilename = buildFilename(student.nim, student.nama, 1, 'Toga', version)
   947→   947→        const ijazahFilename = buildFilename(student.nim, student.nama, 2, 'Ijazah', version)
   948→   948→        console.log(`[SAATIRIL OP] Standard capture v${version} for ${student.nama}: ${togaFilename}, ${ijazahFilename}`)
   949→   949→
   950→   950→        console.log('[SAATIRIL OP] Emitting STUDENT_DONE + PHOTOS_SAVED for student:', student.nama, 'channel:', myChannel, 'version:', version)
   951→   951→
   952→   952→        // PRIORITY 1: STUDENT_DONE — lightweight event for IMMEDIATE MC unblocking
   953→   953→        // This fires BEFORE the heavy PHOTOS_SAVED so MC can call next student instantly
   954→   954→        emitLocal('STUDENT_DONE', {
   955→   955→          studentId: student.id,
   956→   956→          channel: myChannel,
   957→   957→        })
   958→   958→
   959→   959→        // PRIORITY 2: PHOTOS_SAVED — contains photos for Admin gallery + version
   960→   960→        emitLocal('PHOTOS_SAVED', {
   961→   961→          student: { ...student, status: 'done' },
   962→   962→          photos: allPhotos,
   963→   963→          channel: myChannel,
   964→   964→          version,
   965→   965→          filename: togaFilename,
   966→   966→        })
   967→   967→        emitLocal('OP_PROGRESS', { channel: myChannel, status: 'Selesai — Menunggu target...' })
   968→   968→
   969→   969→        // ── SAVE TO DISK ──────────────────────────────────────────────────────
   970→   970→        // Electron (IPC) or Browser (File System Access API) paths.
   971→   971→        const projConfig = useSaatirilStore.getState().currentProject?.config
   972→   972→        const projId = useSaatirilStore.getState().currentProject?.id
   973→   973→        if (projConfig) {
   974→   974→          if (isElectronSaveAvailable()) {
   975→   975→            // ── Electron path ──
   976→   976→            const api = (window as any).saatirilAPI
   977→   977→            const targetFolder = projConfig.targetFolder
   978→   978→            console.log(`[SAATIRIL OP] Saving 2 photos to disk (Electron): ${targetFolder}/`)
   979→   979→            Promise.all([
   980→   980→              api.savePhoto({ base64Data: allPhotos[0], filename: togaFilename, targetFolder }),
   981→   981→              api.savePhoto({ base64Data: allPhotos[1], filename: ijazahFilename, targetFolder }),
   982→   982→            ]).then(([path1, path2]) => {
   983→   983→              if (path1 && path2) {
   984→   984→                console.log(`[SAATIRIL OP] ✓ Photos saved to disk:\n  → ${path1}\n  → ${path2}`)
   985→   985→              } else {
   986→   986→                console.warn(`[SAATIRIL OP] ✗ Some photos FAILED to save: toga=${!!path1} ijazah=${!!path2}`)
   987→   987→                toast({
   988→   988→                  title: 'Gagal Simpan ke Disk',
   989→   989→                  description: 'Sebagian foto tidak tersimpan. Cek ruang disk & folder target.',
   990→   990→                  variant: 'destructive',
   991→   991→                })
   992→   992→              }
   993→   993→            }).catch((err) => {
   994→   994→              console.error('[SAATIRIL OP] Error saving photos to disk:', err)
   995→   995→              toast({
   996→   996→                title: 'Error Simpan Foto',
   997→   997→                description: err.message || 'Terjadi kesalahan saat menyimpan ke disk.',
   998→   998→                variant: 'destructive',
   999→   999→              })
  1000→  1000→            })
  1001→  1001→          } else {
  1002→  1002→            // ── Browser path: File System Access API ──
  1003→  1003→            const storageKey = photoSaveStorageKey(projId)
  1004→  1004→            console.log(`[SAATIRIL OP] Saving 2 photos to disk (Browser FS Access): ${togaFilename}, ${ijazahFilename}`)
  1005→  1005→            Promise.all([
  1006→  1006→              savePhotoInBrowser(storageKey, allPhotos[0], togaFilename),
  1007→  1007→              savePhotoInBrowser(storageKey, allPhotos[1], ijazahFilename),
  1008→  1008→            ]).then(([r1, r2]) => {
  1009→  1009→              if (r1 && r2) {
  1010→  1010→                console.log(`[SAATIRIL OP] ✓ Photos saved to browser folder (v${version})`)
  1011→  1011→                toast({
  1012→  1012→                  title: `Foto Tersimpan (v${version})`,
  1013→  1013→                  description: `${togaFilename} + ${ijazahFilename} — tersimpan ke folder Chrome.`,
  1014→  1014→                })
  1015→  1015→              } else {
  1016→  1016→                console.warn(`[SAATIRIL OP] Browser folder not available — prompting user to pick one`)
  1017→  1017→                toast({
  1018→  1018→                  title: 'Pilih Folder Simpan',
  1019→  1019→                  description: 'Klik tombol "Pilih Folder" lalu foto ulang untuk menyimpan ke disk Chrome.',
  1020→  1020→                  variant: 'destructive',
  1021→  1021→                })
  1022→  1022→              }
  1023→  1023→            }).catch((err: Error) => {
  1024→  1024→              console.error('[SAATIRIL OP] Error saving photos to browser folder:', err)
  1025→  1025→              if (isBrowserSaveSupported()) {
  1026→  1026→                downloadPhotoFallback(allPhotos[0], togaFilename)
  1027→  1027→                downloadPhotoFallback(allPhotos[1], ijazahFilename)
  1028→  1028→              }
  1029→  1029→              toast({
  1030→  1030→                title: 'Error Simpan Foto',
  1031→  1031→                description: err.message || 'Terjadi kesalahan saat menyimpan ke folder Chrome.',
  1032→  1032→                variant: 'destructive',
  1033→  1033→              })
  1034→  1034→            })
  1035→  1035→          }
  1036→  1036→        }
  1037→  1037→
  1038→  1038→        // IMMEDIATE: update local state + emit lightweight SYNC_DB (no delays!)
  1039→  1039→        finishCapture(student, historyItem, newVersions)
  1040→  1040→      }
  1041→  1041→    },
  1042→  1042→    [myChannel, addOpCapturedPhoto, updateStudentStatus, saveProjectsToStorageNow, resetOpState, toast],
  1043→  1043→  )
  1044→  1044→
  1045→  1045→  // ── Photo capture logic ──────────────────────────────────────────────────
  1046→  1046→  const handleCapture = useCallback(() => {
  1047→  1047→    if (!opCurrentTarget) return
  1048→  1048→    if (photoshoot) {
  1049→  1049→      if (capturePhase !== 'ready-1') return
  1050→  1050→    } else {
  1051→  1051→      if (capturePhase !== 'ready-1' && capturePhase !== 'ready-2') return
  1052→  1052→    }
  1053→  1053→    if (isCapturingRef.current) return
  1054→  1054→    isCapturingRef.current = true
  1055→  1055→
  1056→  1056→    const canvas = canvasRef.current
  1057→  1057→    const video = videoRef.current
  1058→  1058→    if (!canvas) return
  1059→  1059→
  1060→  1060→    const targetWidth = 1920
  1061→  1061→    const targetHeight = Math.round(targetWidth / aspectRatio)
  1062→  1062→    canvas.width = targetWidth
  1063→  1063→    canvas.height = targetHeight
  1064→  1064→
  1065→  1065→    const ctx = canvas.getContext('2d')
  1066→  1066→    if (!ctx) return
  1067→  1067→
  1068→  1068→    ctx.fillStyle = '#000000'
  1069→  1069→    ctx.fillRect(0, 0, targetWidth, targetHeight)
  1070→  1070→
  1071→  1071→    if (video && video.readyState >= 2) {
  1072→  1072→      const videoWidth = video.videoWidth
  1073→  1073→      const videoHeight = video.videoHeight
  1074→  1074→      const videoRatio = videoWidth / videoHeight
  1075→  1075→      let sx = 0, sy = 0, sw = videoWidth, sh = videoHeight
  1076→  1076→      if (videoRatio > aspectRatio) {
  1077→  1077→        sw = videoHeight * aspectRatio
  1078→  1078→        sx = (videoWidth - sw) / 2
  1079→  1079→      } else {
  1080→  1080→        sh = videoWidth / aspectRatio
  1081→  1081→        sy = (videoHeight - sh) / 2
  1082→  1082→      }
  1083→  1083→      if (cssFilter !== 'none') ctx.filter = cssFilter
  1084→  1084→      ctx.drawImage(video, sx, sy, sw, sh, 0, 0, targetWidth, targetHeight)
  1085→  1085→      ctx.filter = 'none'
  1086→  1086→    } else {
  1087→  1087→      ctx.fillStyle = '#1a0b2e'
  1088→  1088→      ctx.fillRect(0, 0, targetWidth, targetHeight)
  1089→  1089→      ctx.fillStyle = '#533485'
  1090→  1090→      ctx.font = 'bold 48px sans-serif'
  1091→  1091→      ctx.textAlign = 'center'
  1092→  1092→      ctx.textBaseline = 'middle'
  1093→  1093→      ctx.fillText('NO CAMERA SIGNAL', targetWidth / 2, targetHeight / 2)
  1094→  1094→    }
  1095→  1095→
  1096→  1096→    if (frameImgRef.current) {
  1097→  1097→      ctx.drawImage(frameImgRef.current, 0, 0, targetWidth, targetHeight)
  1098→  1098→      finalizeCapture(canvas)
  1099→  1099→    } else if (frameData) {
  1100→  1100→      const frameImg = new Image()
  1101→  1101→      frameImg.crossOrigin = 'anonymous'
  1102→  1102→      frameImg.onload = () => { ctx.drawImage(frameImg, 0, 0, targetWidth, targetHeight); finalizeCapture(canvas) }
  1103→  1103→      frameImg.onerror = () => { finalizeCapture(canvas) }
  1104→  1104→      frameImg.src = frameData
  1105→  1105→    } else {
  1106→  1106→      finalizeCapture(canvas)
  1107→  1107→    }
  1108→  1108→  }, [opCurrentTarget, capturePhase, aspectRatio, cssFilter, frameData, finalizeCapture, photoshoot])
  1109→  1109→
  1110→  1110→  // ── Shutter: Timer logic ─────────────────────────────────────────────────
  1111→  1111→  const handleCaptureRef = useRef(handleCapture)
  1112→  1112→  useEffect(() => { handleCaptureRef.current = handleCapture }, [handleCapture])
  1113→  1113→  const handleCaptureClickRef = useRef<() => void>(() => {})
  1114→  1114→
  1115→  1115→  const cancelTimer = useCallback(() => {
  1116→  1116→    if (timerIntervalRef.current) {
  1117→  1117→      clearInterval(timerIntervalRef.current)
  1118→  1118→      timerIntervalRef.current = null
  1119→  1119→    }
  1120→  1120→    timerActiveRef.current = false
  1121→  1121→    setTimerCountdown(0)
  1122→  1122→  }, [])
  1123→  1123→
  1124→  1124→  // Cancel timer interval when capture phase changes away from ready (external cleanup only)
  1125→  1125→  useEffect(() => {
  1126→  1126→    if (capturePhase !== 'ready-1' && capturePhase !== 'ready-2') {
  1127→  1127→      if (timerIntervalRef.current) {
  1128→  1128→        clearInterval(timerIntervalRef.current)
  1129→  1129→        timerIntervalRef.current = null
  1130→  1130→      }
  1131→  1131→      timerActiveRef.current = false
  1132→  1132→    }
  1133→  1133→  }, [capturePhase])
  1134→  1134→
  1135→  1135→  // Derived: only show countdown when in ready phase
  1136→  1136→  const effectiveTimerCountdown = (capturePhase === 'ready-1' || capturePhase === 'ready-2') ? timerCountdown : 0
  1137→  1137→
  1138→  1138→  const startTimer = useCallback(() => {
  1139→  1139→    if (!isTimerMode(effectiveShutterMode)) return
  1140→  1140→    if (capturePhase !== 'ready-1' && capturePhase !== 'ready-2') return
  1141→  1141→    if (timerActiveRef.current) {
  1142→  1142→      // Cancel if already running
  1143→  1143→      cancelTimer()
  1144→  1144→      return
  1145→  1145→    }
  1146→  1146→
  1147→  1147→    const duration = getTimerDuration(effectiveShutterMode)
  1148→  1148→    let remaining = duration
  1149→  1149→    timerActiveRef.current = true
  1150→  1150→    setTimerCountdown(remaining)
  1151→  1151→
  1152→  1152→    timerIntervalRef.current = setInterval(() => {
  1153→  1153→      remaining -= 1
  1154→  1154→      if (remaining <= 0) {
  1155→  1155→        // Time's up — capture!
  1156→  1156→        if (timerIntervalRef.current) {
  1157→  1157→          clearInterval(timerIntervalRef.current)
  1158→  1158→          timerIntervalRef.current = null
  1159→  1159→        }
  1160→  1160→        timerActiveRef.current = false
  1161→  1161→        setTimerCountdown(0)
  1162→  1162→        handleCaptureRef.current()
  1163→  1163→      } else {
  1164→  1164→        setTimerCountdown(remaining)
  1165→  1165→      }
  1166→  1166→    }, 1000)
  1167→  1167→  }, [effectiveShutterMode, capturePhase, cancelTimer])
  1168→  1168→
  1169→  1169→  // ── Shutter: AI detection ────────────────────────────────────────────────
  1170→  1170→  const capturePhaseRef = useRef(capturePhase)
  1171→  1171→  useEffect(() => { capturePhaseRef.current = capturePhase }, [capturePhase])
  1172→  1172→
  1173→  1173→  // AI: Initialize when camera is ready and AI shutter mode is active
  1174→  1174→  useEffect(() => {
  1175→  1175→    if (effectiveShutterMode === 'ai' && cameraAvailable && hasActiveTarget && !ai.scriptsLoaded && ai.status === 'unloaded') {
  1176→  1176→      ai.initialize().then((ok) => {
  1177→  1177→        if (ok) console.log('[SAATIRIL OP] AI initialized for shutter mode')
  1178→  1178→      })
  1179→  1179→    }
  1180→  1180→  }, [effectiveShutterMode, cameraAvailable, hasActiveTarget])
  1181→  1181→
  1182→  1182→  // AI: Start/stop detection based on shutter mode
  1183→  1183→  useEffect(() => {
  1184→  1184→    if (effectiveShutterMode === 'ai' && ai.modelLoaded && cameraAvailable && videoRef.current && hasActiveTarget) {
  1185→  1185→      ai.startDetection(videoRef.current, (event: AIMomentEvent) => {
  1186→  1186→        console.log('[SAATIRIL OP] AI moment:', event.type, 'phase:', capturePhaseRef.current)
  1187→  1187→        const phase = capturePhaseRef.current
  1188→  1188→        if (event.type === 'toga' && phase === 'ready-1') {
  1189→  1189→          handleCaptureRef.current()
  1190→  1190→        } else if (event.type === 'ijazah' && phase === 'ready-2') {
  1191→  1191→          handleCaptureRef.current()
  1192→  1192→        }
  1193→  1193→      })
  1194→  1194→    } else if (effectiveShutterMode !== 'ai' && ai.isRunning) {
  1195→  1195→      ai.stopDetection()
  1196→  1196→    }
  1197→  1197→  }, [effectiveShutterMode, ai.modelLoaded, cameraAvailable, hasActiveTarget, capturePhase])
  1198→  1198→
  1199→  1199→  // ── Palm trigger (hands-free shutter) ────────────────────────────────────
  1200→  1200→  // Palm detection is a TRIGGER, not a shutter mode. When the toggle is ON,
  1201→  1201→  // showing an open palm fires handleCaptureButtonClick() — which respects
  1202→  1202→  // the selected mode (manual → instant photo, timer → starts countdown).
  1203→  1203→  // Disabled in AI mode (AI already auto-triggers).
  1204→  1204→  const palmTriggerActive = palmTriggerEnabled && effectiveShutterMode !== 'ai'
  1205→  1205→
  1206→  1206→  useEffect(() => {
  1207→  1207→    if (palmTriggerActive && cameraAvailable && hasActiveTarget && palm.status === 'unloaded') {
  1208→  1208→      palm.initialize().then((ok) => {
  1209→  1209→        if (ok) console.log('[SAATIRIL OP] Palm trigger initialized')
  1210→  1210→      })
  1211→  1211→    }
  1212→  1212→  }, [palmTriggerActive, cameraAvailable, hasActiveTarget])
  1213→  1213→
  1214→  1214→  useEffect(() => {
  1215→  1215→    if (palmTriggerActive && (palm.status === 'model_ready' || palm.status === 'stopped') && cameraAvailable && videoRef.current && hasActiveTarget) {
  1216→  1216→      palm.startDetection(videoRef.current, {
  1217→  1217→        onPalmConfirmed: () => {
  1218→  1218→          console.log('[SAATIRIL OP] Palm confirmed — triggering shutter')
  1219→  1219→          handleCaptureClickRef.current()
  1220→  1220→        },
  1221→  1221→        onPalmReleased: () => {
  1222→  1222→          // No-op: let the selected mode run to completion.
  1223→  1223→        },
  1224→  1224→      })
  1225→  1225→    } else if (!palmTriggerActive && palm.isRunning) {
  1226→  1226→      palm.stopDetection()
  1227→  1227→    }
  1228→  1228→  }, [palmTriggerActive, palm.status, cameraAvailable, hasActiveTarget])
  1229→  1229→
  1230→  1230→  // ── Cleanup on unmount ───────────────────────────────────────────────────
  1231→  1231→  useEffect(() => {
  1232→  1232→    return () => {
  1233→  1233→      cancelTimer()
  1234→  1234→      if (ai.isRunning) ai.stopDetection()
  1235→  1235→      if (palm.isRunning) palm.stopDetection()
  1236→  1236→    }
  1237→  1237→  }, [])
  1238→  1238→
  1239→  1239→  // ── Shutter mode: determine if capture button should trigger timer or direct capture
  1240→  1240→  const handleCaptureButtonClick = useCallback(() => {
  1241→  1241→    if (isTimerMode(effectiveShutterMode)) {
  1242→  1242→      if (timerActiveRef.current) {
  1243→  1243→        // Cancel running timer
  1244→  1244→        cancelTimer()
  1245→  1245→      } else {
  1246→  1246→        // Start timer
  1247→  1247→        startTimer()
  1248→  1248→      }
  1249→  1249→    } else {
  1250→  1250→      // Manual mode — direct capture
  1251→  1251→      handleCapture()
  1252→  1252→    }
  1253→  1253→  }, [effectiveShutterMode, startTimer, cancelTimer, handleCapture])
  1254→  1254→
  1255→  1255→  // Keep handleCaptureClickRef in sync with the latest handler
  1256→  1256→  useEffect(() => { handleCaptureClickRef.current = handleCaptureButtonClick }, [handleCaptureButtonClick])
  1257→  1257→
  1258→  1258→  // ── Keyboard shortcuts (physical shutter for camera operator) ──────────────
  1259→  1259→  // Space / Enter → trigger capture (same as clicking the FOTO button)
  1260→  1260→  // Esc           → cancel a running countdown timer
  1261→  1261→  // F             → toggle browser fullscreen (hands-free operation)
  1262→  1262→  const cancelTimerRef = useRef(cancelTimer)
  1263→  1263→  useEffect(() => { cancelTimerRef.current = cancelTimer }, [cancelTimer])
  1264→  1264→  const [isFullscreen, setIsFullscreen] = useState(false)
  1265→  1265→
  1266→  1266→  useEffect(() => {
  1267→  1267→    if (readOnly) return // Monitor mode: no physical shutter
  1268→  1268→
  1269→  1269→    const isTypingTarget = (el: EventTarget | null): boolean => {
  1270→  1270→      if (!(el instanceof HTMLElement)) return false
  1271→  1271→      const tag = el.tagName
  1272→  1272→      return tag === 'INPUT' || tag === 'TEXTAREA' || tag === 'SELECT' || el.isContentEditable
  1273→  1273→    }
  1274→  1274→
  1275→  1275→    const toggleFullscreen = () => {
  1276→  1276→      const docEl = document.documentElement
  1277→  1277→      if (!document.fullscreenElement) {
  1278→  1278→        docEl.requestFullscreen?.().then(() => setIsFullscreen(true)).catch(() => {})
  1279→  1279→      } else {
  1280→  1280→        document.exitFullscreen?.().then(() => setIsFullscreen(false)).catch(() => {})
  1281→  1281→      }
  1282→  1282→    }
  1283→  1283→
  1284→  1284→    const onKeyDown = (e: KeyboardEvent) => {
  1285→  1285→      if (isTypingTarget(e.target)) return
  1286→  1286→
  1287→  1287→      if (e.key === 'Escape') {
  1288→  1288→        if (timerActiveRef.current) {
  1289→  1289→          e.preventDefault()
  1290→  1290→          cancelTimerRef.current()
  1291→  1291→        }
  1292→  1292→        return
  1293→  1293→      }
  1294→  1294→
  1295→  1295→      if (e.key === ' ' || e.key === 'Enter') {
  1296→  1296→        e.preventDefault()
  1297→  1297→        handleCaptureClickRef.current()
  1298→  1298→        return
  1299→  1299→      }
  1300→  1300→
  1301→  1301→      if (e.key === 'f' || e.key === 'F') {
  1302→  1302→        e.preventDefault()
  1303→  1303→        toggleFullscreen()
  1304→  1304→        return
  1305→  1305→      }
  1306→  1306→    }
  1307→  1307→
  1308→  1308→    const onFullscreenChange = () => {
  1309→  1309→      setIsFullscreen(!!document.fullscreenElement)
  1310→  1310→    }
  1311→  1311→
  1312→  1312→    window.addEventListener('keydown', onKeyDown)
  1313→  1313→    document.addEventListener('fullscreenchange', onFullscreenChange)
  1314→  1314→    return () => {
  1315→  1315→      window.removeEventListener('keydown', onKeyDown)
  1316→  1316→      document.removeEventListener('fullscreenchange', onFullscreenChange)
  1317→  1317→    }
  1318→  1318→  }, [readOnly])
  1319→  1319→
  1320→  1320→  // ── Progress badge text ──────────────────────────────────────────────────
  1321→  1321→  const progressText = useMemo(() => {
  1322→  1322→    if (!hasActiveTarget) return 'Menunggu Arahan MC...'
  1323→  1323→    if (effectiveTimerCountdown > 0) return `Timer: ${effectiveTimerCountdown}s`
  1324→  1324→    if (effectiveShutterMode === 'ai' && ai.isRunning) {
  1325→  1325→      if (ai.momentState === 'toga_possible' || ai.momentState === 'toga_sustained') return 'AI: Toga terdeteksi...'
  1326→  1326→      if (ai.momentState === 'ijazah_possible' || ai.momentState === 'ijazah_sustained') return 'AI: Ijazah terdeteksi...'
  1327→  1327→    }
  1328→  1328→    if (palmTriggerEnabled && palm.isRunning) {
  1329→  1329→      if (palm.palmState === 'confirmed') return 'Telapak terdeteksi — memicu shutter...'
  1330→  1330→      if (palm.palmState === 'held') return 'Telapak terdeteksi...'
  1331→  1331→      if (palm.palmState === 'searching') return 'Mencari telapak tangan...'
  1332→  1332→    }
  1333→  1333→    if (photoshoot) {
  1334→  1334→      if (capturePhase === 'ready-1') return 'Siap Foto'
  1335→  1335→      if (capturePhase === 'sending') return 'Mengirim...'
  1336→  1336→    } else {
  1337→  1337→      if (capturePhase === 'ready-1') return 'Siap Foto 1'
  1338→  1338→      if (capturePhase === 'ready-2') return 'Pose 1 OK - Siap Foto 2'
  1339→  1339→      if (capturePhase === 'sending') return 'Mengirim...'
  1340→  1340→    }
  1341→  1341→    return 'Menunggu Arahan MC...'
  1342→  1342→  }, [hasActiveTarget, capturePhase, photoshoot, effectiveTimerCountdown, effectiveShutterMode, ai.isRunning, ai.momentState, palmTriggerEnabled, palm.isRunning, palm.palmState])
  1343→  1343→
  1344→  1344→  // ── Render helpers ───────────────────────────────────────────────────────
  1345→  1345→  const getRowStyle = (student: Student): React.CSSProperties => {
  1346→  1346→    const isActive = student.status === `active_${myChannel}`
  1347→  1347→    const isSent = student.status === 'sent'
  1348→  1348→    const isNext = !photoshoot && student.id === nextPending?.id && student.status === 'pending'
  1349→  1349→    const isDone = student.status === 'done'
  1350→  1350→    if (isActive) return { backgroundColor: `${THEME.gold}22`, borderLeft: `4px solid ${THEME.gold}`, boxShadow: `0 0 12px ${THEME.gold}44` }
  1351→  1351→    if (isSent) return { backgroundColor: `${THEME.gold}0a`, borderLeft: `4px solid ${THEME.gold}88` }
  1352→  1352→    if (isNext) return { backgroundColor: THEME.panel, borderLeft: `4px solid ${THEME.gold}` }
  1353→  1353→    if (isDone) return { backgroundColor: '#22c55e0d', opacity: 0.55, borderLeft: `4px solid #22c55e66` }
  1354→  1354→    return { backgroundColor: THEME.panel, borderLeft: `4px solid ${THEME.border}` }
  1355→  1355→  }
  1356→  1356→
  1357→  1357→  const renderStatusBadge = (status: StudentStatus) => {
  1358→  1358→    if (status === 'done') return <Badge className="text-[10px] px-1.5 py-0" style={{ backgroundColor: '#22c55e33', color: '#4ade80', border: '1px solid #22c55e55' }}><CheckCircle2 className="size-3 mr-0.5" />Selesai</Badge>
  1359→  1359→    if (status === 'sent') return <Badge className="text-[10px] px-1.5 py-0" style={{ backgroundColor: '#d4af3733', color: THEME.gold, border: '1px solid #d4af3766' }}><Camera className="size-3 mr-0.5" />Dikirim</Badge>
  1360→  1360→    if (isActiveStatus(status)) return <Badge className="text-[10px] px-1.5 py-0 animate-pulse" style={{ backgroundColor: `${THEME.gold}33`, color: THEME.gold, border: `1px solid ${THEME.gold}66` }}><Loader2 className="size-3 mr-0.5 animate-spin" />{statusLabel(status)}</Badge>
  1361→  1361→    return <Badge className="text-[10px] px-1.5 py-0" style={{ backgroundColor: `${THEME.border}44`, color: THEME.muted, border: `1px solid ${THEME.border}` }}><Clock className="size-3 mr-0.5" />Menunggu</Badge>
  1362→  1362→  }
  1363→  1363→
  1364→  1364→  // ── Shutter mode selector ────────────────────────────────────────────────
  1365→  1365→  const renderShutterModeSelector = (compact = false) => {
  1366→  1366→    const availableModes = SHUTTER_MODES.filter((m) => {
  1367→  1367→      if (m.modesAllowed && !m.modesAllowed.includes(mode as CameraMode)) return false
  1368→  1368→      return true
  1369→  1369→    })
  1370→  1370→
  1371→  1371→    return (
  1372→  1372→      <div className={`flex flex-col gap-1 ${compact ? '' : ''}`}>
  1373→  1373→        <p className={`font-semibold uppercase tracking-widest ${compact ? 'text-[8px]' : 'text-[9px]'}`} style={{ color: THEME.muted }}>
  1374→  1374→          Mode Shutter
  1375→  1375→        </p>
  1376→  1376→        <div className={`flex flex-wrap gap-1 ${compact ? '' : ''}`}>
  1377→  1377→          {availableModes.map((m) => {
  1378→  1378→            const isActive = effectiveShutterMode === m.id
  1379→  1379→            const isLoading = (m.id === 'ai' && (ai.status === 'loading_scripts' || ai.status === 'loading_model'))
  1380→  1380→
  1381→  1381→            return (
  1382→  1382→              <button
  1383→  1383→                key={m.id}
  1384→  1384→                onClick={() => {
  1385→  1385→                  if (!isLoading) {
  1386→  1386→                    setShutterMode(m.id)
  1387→  1387→                    cancelTimer()
  1388→  1388→                  }
  1389→  1389→                }}
  1390→  1390→                className={`flex items-center gap-1 rounded-md font-semibold transition-all duration-200 cursor-pointer ${
  1391→  1391→                  compact ? 'px-1.5 py-1 text-[9px]' : 'px-2 py-1.5 text-[10px]'
  1392→  1392→                } ${isActive ? 'scale-105' : 'hover:bg-white/5'}`}
  1393→  1393→                style={{
  1394→  1394→                  backgroundColor: isActive ? `${THEME.gold}33` : THEME.panel,
  1395→  1395→                  color: isActive ? THEME.gold : THEME.muted,
  1396→  1396→                  border: `1px solid ${isActive ? THEME.gold : THEME.border}`,
  1397→  1397→                  boxShadow: isActive ? `0 0 8px ${THEME.gold}22` : 'none',
  1398→  1398→                  opacity: isLoading ? 0.6 : 1,
  1399→  1399→                }}
  1400→  1400→                title={m.label}
  1401→  1401→              >
  1402→  1402→                {isLoading ? <Loader2 className="size-3 animate-spin" /> : m.icon}
  1403→  1403→                <span>{m.shortLabel}</span>
  1404→  1404→              </button>
  1405→  1405→            )
  1406→  1406→          })}
  1407→  1407→        </div>
  1408→  1408→        {/* Palm trigger toggle — independent of shutter mode */}
  1409→  1409→        {!readOnly && effectiveShutterMode !== 'ai' && (
  1410→  1410→          <button
  1411→  1411→            onClick={() => {
  1412→  1412→              setPalmTriggerEnabled((v) => !v)
  1413→  1413→              if (palmTriggerEnabled) palm.stopDetection()
  1414→  1414→            }}
  1415→  1415→            className={`flex items-center gap-1.5 rounded-md font-semibold transition-all duration-200 cursor-pointer ${
  1416→  1416→              compact ? 'px-1.5 py-1 text-[9px]' : 'px-2 py-1.5 text-[10px]'
  1417→  1417→            } ${palmTriggerEnabled ? 'scale-105' : 'hover:bg-white/5'}`}
  1418→  1418→            style={{
  1419→  1419→              backgroundColor: palmTriggerEnabled ? '#22c55e22' : THEME.panel,
  1420→  1420→              color: palmTriggerEnabled ? '#4ade80' : THEME.muted,
  1421→  1421→              border: `1px solid ${palmTriggerEnabled ? '#22c55e' : THEME.border}`,
  1422→  1422→              boxShadow: palmTriggerEnabled ? '0 0 8px #22c55e22' : 'none',
  1423→  1423→            }}
  1424→  1424→            title="Trigger Telapak — tunjukkan telapak tangan ke kamera untuk memotret"
  1425→  1425→          >
  1426→  1426→            {palm.status === 'loading_scripts' || palm.status === 'loading_model' ? (
  1427→  1427→              <Loader2 className="size-3 animate-spin" />
  1428→  1428→            ) : (
  1429→  1429→              <Hand className="size-3" />
  1430→  1430→            )}
  1431→  1431→            <span>Trigger Telapak</span>
  1432→  1432→            {palmTriggerEnabled && palm.isRunning && palm.fingersExtended > 0 && (
  1433→  1433→              <span className="text-[8px] font-mono" style={{ color: palm.palmState === 'confirmed' ? '#4ade80' : THEME.gold }}>
  1434→  1434→                {palm.fingersExtended}/5
  1435→  1435→              </span>
  1436→  1436→            )}
  1437→  1437→          </button>
  1438→  1438→        )}
  1439→  1439→      </div>
  1440→  1440→    )
  1441→  1441→  }
  1442→  1442→
  1443→  1443→  // ── Operator queue with search (photoshoot mode) ────────────────────────
  1444→  1444→  const renderOpSearch = (compact = false) => {
  1445→  1445→    if (!photoshoot) return null
  1446→  1446→    return (
  1447→  1447→      <Card
  1448→  1448→        className="shrink-0 border rounded-lg overflow-hidden"
  1449→  1449→        style={{ backgroundColor: THEME.card, borderColor: THEME.gold }}
  1450→  1450→      >
  1451→  1451→        <CardContent className={compact ? 'p-2 space-y-1.5' : 'p-2.5 space-y-2'}>
  1452→  1452→          <div className="flex items-center justify-between">
  1453→  1453→            <p
  1454→  1454→              className="text-[9px] font-semibold uppercase tracking-widest"
  1455→  1455→              style={{ color: THEME.gold }}
  1456→  1456→            >
  1457→  1457→              <List className="size-3 inline mr-1" />
  1458→  1458→              Antre dari MC ({opQueue.length})
  1459→  1459→            </p>
  1460→  1460→          </div>
  1461→  1461→
  1462→  1462→          <div className="relative">
  1463→  1463→            <Search className="absolute left-2 top-1/2 -translate-y-1/2 size-3.5" style={{ color: THEME.muted }} />
  1464→  1464→            <Input
  1465→  1465→              placeholder="Cari NIM / Nama di antrean..."
  1466→  1466→              value={opSearchQuery}
  1467→  1467→              onChange={(e) => setOpSearchQuery(e.target.value)}
  1468→  1468→              className={`pl-7 border-[#533485] bg-[#2a164a] text-white placeholder:text-[#533485] focus-visible:border-[#d4af37] focus-visible:ring-[#d4af37]/30 ${compact ? 'h-7 text-[11px]' : 'h-8 text-xs'}`}
  1469→  1469→            />
  1470→  1470→          </div>
  1471→  1471→
  1472→  1472→          {opQueue.length === 0 ? (
  1473→  1473→            <p className={`text-center ${compact ? 'text-[9px] py-1' : 'text-[10px] py-1.5'}`} style={{ color: THEME.muted }}>
  1474→  1474→              {opSearchQuery.trim() ? 'Tidak ditemukan di antrean' : 'Belum ada peserta dikirim MC'}
  1475→  1475→            </p>
  1476→  1476→          ) : (
  1477→  1477→            <div
  1478→  1478→              className={`overflow-y-auto rounded-md border ${compact ? 'max-h-32' : 'max-h-48'}`}
  1479→  1479→              style={{ borderColor: THEME.border }}
  1480→  1480→            >
  1481→  1481→              {opSearchResults.map((student) => (
  1482→  1482→                <button
  1483→  1483→                  key={student.id}
  1484→  1484→                  onClick={() => {
  1485→  1485→                    if (!sending) {
  1486→  1486→                      setOpCurrentTarget(student)
  1487→  1487→                      setOpSearchQuery('')
  1488→  1488→                    }
  1489→  1489→                  }}
  1490→  1490→                  className={`w-full flex items-center gap-1.5 px-2 text-left transition-colors hover:bg-white/5 cursor-pointer ${compact ? 'py-1' : 'py-1.5'} ${opCurrentTarget?.id === student.id ? 'bg-[#d4af37]/10' : ''}`}
  1491→  1491→                  style={{
  1492→  1492→                    borderBottom: `1px solid ${THEME.border}44`,
  1493→  1493→                    borderLeft: opCurrentTarget?.id === student.id ? `3px solid ${THEME.gold}` : `3px solid transparent`,
  1494→  1494→                  }}
  1495→  1495→                >
  1496→  1496→                  <span className={`font-mono truncate shrink-0 ${compact ? 'text-[9px] w-12' : 'text-[10px] w-14'}`} style={{ color: THEME.muted }}>
  1497→  1497→                    {student.nim}
  1498→  1498→                  </span>
  1499→  1499→                  <span className={`font-medium truncate flex-1 ${compact ? 'text-[10px]' : 'text-[11px]'}`} style={{ color: opCurrentTarget?.id === student.id ? THEME.gold : '#ffffff' }}>
  1500→  1500→                    {student.nama}
  1501→  1501→                  </span>
  1502→  1502→                  {opCurrentTarget?.id === student.id && (
  1503→  1503→                    <Camera className="size-3 shrink-0" style={{ color: THEME.gold }} />
  1504→  1504→                  )}
  1505→  1505→                </button>
  1506→  1506→              ))}
  1507→  1507→            </div>
  1508→  1508→          )}
  1509→  1509→        </CardContent>
  1510→  1510→      </Card>
  1511→  1511→    )
  1512→  1512→  }
  1513→  1513→
  1514→  1514→  // ── Shared camera view component ─────────────────────────────────────────
  1515→  1515→  const renderCameraView = () => (
  1516→  1516→    <div
  1517→  1517→      className="relative rounded-xl overflow-hidden border-2"
  1518→  1518→      style={{
  1519→  1519→        width: cameraDims.width > 0 ? cameraDims.width : undefined,
  1520→  1520→        height: cameraDims.height > 0 ? cameraDims.height : undefined,
  1521→  1521→        borderColor: hasActiveTarget ? THEME.gold : THEME.border,
  1522→  1522→        boxShadow: hasActiveTarget ? `0 0 16px ${THEME.gold}15` : 'none',
  1523→  1523→        backgroundColor: '#000000',
  1524→  1524→      }}
  1525→  1525→    >
  1526→  1526→      <video
  1527→  1527→        ref={videoRef}
  1528→  1528→        autoPlay
  1529→  1529→        playsInline
  1530→  1530→        muted
  1531→  1531→        className="absolute inset-0 w-full h-full object-cover"
  1532→  1532→        style={{
  1533→  1533→          filter: cssFilter !== 'none' ? cssFilter : undefined,
  1534→  1534→        }}
  1535→  1535→      />
  1536→  1536→
  1537→  1537→      {frameData && (
  1538→  1538→        <img
  1539→  1539→          src={frameData}
  1540→  1540→          alt="Frame overlay"
  1541→  1541→          className="absolute inset-0 w-full h-full object-fill pointer-events-none"
  1542→  1542→          style={{ zIndex: 5 }}
  1543→  1543→        />
  1544→  1544→      )}
  1545→  1545→
  1546→  1546→      {/* Rule of thirds grid */}
  1547→  1547→      <div className="absolute inset-0 pointer-events-none" style={{ zIndex: 6 }}>
  1548→  1548→        <div className="absolute top-0 bottom-0 left-[33.333%] w-px bg-white/[0.12]" />
  1549→  1549→        <div className="absolute top-0 bottom-0 left-[66.666%] w-px bg-white/[0.12]" />
  1550→  1550→        <div className="absolute left-0 right-0 top-[33.333%] h-px bg-white/[0.12]" />
  1551→  1551→        <div className="absolute left-0 right-0 top-[66.666%] h-px bg-white/[0.12]" />
  1552→  1552→      </div>
  1553→  1553→
  1554→  1554→      {/* Timer countdown overlay */}
  1555→  1555→      {effectiveTimerCountdown > 0 && (
  1556→  1556→        <div className="absolute inset-0 flex items-center justify-center pointer-events-none" style={{ zIndex: 15 }}>
  1557→  1557→          <div className="flex items-center justify-center" style={{
  1558→  1558→            width: isMobile ? '80px' : '120px',
  1559→  1559→            height: isMobile ? '80px' : '120px',
  1560→  1560→            borderRadius: '50%',
  1561→  1561→            backgroundColor: `${THEME.bg}cc`,
  1562→  1562→            border: `4px solid ${THEME.gold}`,
  1563→  1563→            boxShadow: `0 0 40px ${THEME.gold}66, 0 0 80px ${THEME.gold}33`,
  1564→  1564→          }}>
  1565→  1565→            <span className="font-bold" style={{
  1566→  1566→              color: THEME.gold,
  1567→  1567→              fontSize: isMobile ? '36px' : '56px',
  1568→  1568→              textShadow: `0 0 20px ${THEME.gold}88`,
  1569→  1569→            }}>
  1570→  1570→              {effectiveTimerCountdown}
  1571→  1571→            </span>
  1572→  1572→          </div>
  1573→  1573→        </div>
  1574→  1574→      )}
  1575→  1575→
  1576→  1576→      {/* Palm trigger indicator */}
  1577→  1577→      {palmTriggerEnabled && palm.isRunning && (
  1578→  1578→        <div className="absolute top-2 right-2 flex items-center gap-1.5 pointer-events-none" style={{ zIndex: 10 }}>
  1579→  1579→          <div className="flex items-center gap-1 rounded-full px-2 py-1" style={{
  1580→  1580→            backgroundColor: palm.palmState === 'confirmed' ? '#22c55e88' : palm.palmState === 'held' ? '#d4af3788' : 'rgba(0,0,0,0.7)',
  1581→  1581→            border: `1px solid ${palm.palmState === 'confirmed' ? '#22c55e' : palm.palmState === 'held' ? THEME.gold : THEME.border}`,
  1582→  1582→          }}>
  1583→  1583→            <Hand className={`size-3 ${palm.palmState === 'held' ? 'animate-pulse' : ''}`} style={{ color: palm.palmState === 'confirmed' ? '#4ade80' : THEME.gold }} />
  1584→  1584→            <span className="text-[10px] font-bold" style={{ color: palm.palmState === 'confirmed' ? '#4ade80' : THEME.muted }}>
  1585→  1585→              {palm.palmState === 'confirmed' ? 'OK' : palm.palmState === 'held' ? '...' : palm.fingersExtended > 0 ? `${palm.fingersExtended}/5` : 'Telapak'}
  1586→  1586→            </span>
  1587→  1587→          </div>
  1588→  1588→        </div>
  1589→  1589→      )}
  1590→  1590→
  1591→  1591→      {/* AI detection indicator */}
  1592→  1592→      {effectiveShutterMode === 'ai' && ai.isRunning && (
  1593→  1593→        <div className="absolute top-2 right-2 flex items-center gap-1.5 pointer-events-none" style={{ zIndex: 10 }}>
  1594→  1594→          <div className="flex items-center gap-1 rounded-full px-2 py-1" style={{
  1595→  1595→            backgroundColor: ai.momentState !== 'idle' ? '#d4af3788' : 'rgba(0,0,0,0.7)',
  1596→  1596→            border: `1px solid ${ai.momentState !== 'idle' ? THEME.gold : THEME.border}`,
  1597→  1597→          }}>
  1598→  1598→            <Sparkles className={`size-3 ${ai.momentState !== 'idle' ? 'animate-pulse' : ''}`} style={{ color: ai.momentState !== 'idle' ? THEME.gold : THEME.muted }} />
  1599→  1599→            <span className="text-[10px] font-bold" style={{ color: ai.momentState !== 'idle' ? THEME.gold : THEME.muted }}>
  1600→  1600→              AI {ai.posesDetected > 0 ? `(${ai.posesDetected})` : ''}
  1601→  1601→            </span>
  1602→  1602→          </div>
  1603→  1603→        </div>
  1604→  1604→      )}
  1605→  1605→
  1606→  1606→      {/* Flash overlay */}
  1607→  1607→      <div
  1608→  1608→        ref={flashRef}
  1609→  1609→        className="absolute inset-0 transition-opacity duration-150 pointer-events-none"
  1610→  1610→        style={{
  1611→  1611→          backgroundColor: '#ffffff',
  1612→  1612→          opacity: flashVisible ? 0.85 : 0,
  1613→  1613→          zIndex: 20,
  1614→  1614→        }}
  1615→  1615→      />
  1616→  1616→
  1617→  1617→      {/* NO CAMERA SIGNAL */}
  1618→  1618→      {!cameraAvailable && (
  1619→  1619→        <div className="absolute inset-0 flex flex-col items-center justify-center gap-2 bg-black/80" style={{ zIndex: 8 }}>
  1620→  1620→          <VideoOff className={isMobile ? 'size-8' : 'size-12'} style={{ color: THEME.border }} />
  1621→  1621→          <p className={`font-semibold tracking-wider ${isMobile ? 'text-xs' : 'text-sm'}`} style={{ color: THEME.muted }}>
  1622→  1622→            NO CAMERA SIGNAL
  1623→  1623→          </p>
  1624→  1624→          <p className="text-[10px]" style={{ color: THEME.border }}>
  1625→  1625→            Pastikan kamera terhubung dan izin diberikan
  1626→  1626→          </p>
  1627→  1627→        </div>
  1628→  1628→      )}
  1629→  1629→
  1630→  1630→      {/* Aspect ratio & frame badges */}
  1631→  1631→      <div className="absolute top-2 left-2 flex gap-1.5" style={{ zIndex: 10 }}>
  1632→  1632→        <Badge className="text-[9px] px-1.5 py-0.5 border-0" style={{ backgroundColor: 'rgba(0,0,0,0.6)', color: THEME.muted }}>
  1633→  1633→          {config?.ratio ?? '4:3'}
  1634→  1634→        </Badge>
  1635→  1635→        {frameData && (
  1636→  1636→          <Badge className="text-[9px] px-1.5 py-0.5 border-0" style={{ backgroundColor: 'rgba(0,0,0,0.6)', color: THEME.gold }}>
  1637→  1637→            <Frame className="size-2.5 mr-0.5" />Frame
  1638→  1638→          </Badge>
  1639→  1639→        )}
  1640→  1640→      </div>
  1641→  1641→
  1642→  1642→      {/* Mobile: Switch camera button */}
  1643→  1643→      {isMobile && videoDevices.length > 1 && (
  1644→  1644→        <button
  1645→  1645→          onClick={handleSwitchCamera}
  1646→  1646→          className="absolute top-2 right-2 flex items-center justify-center w-9 h-9 rounded-full bg-black/60 backdrop-blur-sm cursor-pointer active:scale-95 transition-transform"
  1647→  1647→          style={{ zIndex: 10 }}
  1648→  1648→          title="Ganti kamera"
  1649→  1649→        >
  1650→  1650→          <SwitchCamera className="size-4" style={{ color: THEME.gold }} />
  1651→  1651→        </button>
  1652→  1652→      )}
  1653→  1653→
  1654→  1654→      <canvas ref={canvasRef} className="hidden" />
  1655→  1655→    </div>
  1656→  1656→  )
  1657→  1657→
  1658→  1658→  // ── Capture button (shared) ──────────────────────────────────────────────
  1659→  1659→  const renderCaptureButton = (size: 'normal' | 'large' | 'xl' = 'normal') => {
  1660→  1660→    const btnClass = size === 'xl'
  1661→  1661→      ? 'w-full h-16 sm:h-20 text-lg sm:text-xl font-bold cursor-pointer rounded-xl transition-all duration-200 active:scale-[0.97] shadow-lg'
  1662→  1662→      : size === 'large'
  1663→  1663→        ? 'w-full h-14 text-base font-bold cursor-pointer rounded-lg transition-all duration-200 active:scale-[0.97]'
  1664→  1664→        : 'w-full h-12 text-sm font-bold cursor-pointer rounded-lg transition-all duration-200 hover:scale-[1.01] active:scale-[0.99]'
  1665→  1665→    // Icon size scales with button size so the shutter is easy to hit
  1666→  1666→    const iconCls = size === 'xl' ? 'size-6 sm:size-7' : 'size-4'
  1667→  1667→
  1668→  1668→    if (readOnly) {
  1669→  1669→      return (
  1670→  1670→        <Button disabled className={`${btnClass} cursor-not-allowed`} style={{ backgroundColor: THEME.panel, color: THEME.muted, border: `2px solid ${THEME.border}`, opacity: 0.6 }}>
  1671→  1671→          <Monitor className={`${iconCls} mr-2`} />MODE MONITOR
  1672→  1672→        </Button>
  1673→  1673→      )
  1674→  1674→    }
  1675→  1675→
  1676→  1676→    if (capturePhase === 'standby') {
  1677→  1677→      return (
  1678→  1678→        <Button disabled className={`${btnClass} cursor-not-allowed`} style={{ backgroundColor: THEME.panel, color: THEME.muted, border: `2px solid ${THEME.border}`, opacity: 0.6 }}>
  1679→  1679→          <Aperture className={`${iconCls} mr-2`} />STANDBY
  1680→  1680→        </Button>
  1681→  1681→      )
  1682→  1682→    }
  1683→  1683→
  1684→  1684→    // Timer is counting down — show cancel button
  1685→  1685→    if (effectiveTimerCountdown > 0) {
  1686→  1686→      return (
  1687→  1687→        <Button onClick={handleCaptureButtonClick} className={btnClass} style={{
  1688→  1688→          backgroundColor: '#ef4444',
  1689→  1689→          color: '#ffffff',
  1690→  1690→          border: '2px solid #ef4444',
  1691→  1691→          boxShadow: '0 0 30px #ef444444, 0 0 60px #ef444422',
  1692→  1692→        }}>
  1693→  1693→          <X className={`${iconCls} mr-2`} />BATAL ({effectiveTimerCountdown}s)
  1694→  1694→        </Button>
  1695→  1695→      )
  1696→  1696→    }
  1697→  1697→
  1698→  1698→    if (capturePhase === 'ready-1') {
  1699→  1699→      const isAutoMode = effectiveShutterMode === 'ai'
  1700→  1700→      const isTimer = isTimerMode(effectiveShutterMode)
  1701→  1701→
  1702→  1702→      if (isAutoMode) {
  1703→  1703→        // AI auto-capture mode: show status button
  1704→  1704→        const isDetecting = ai.isRunning
  1705→  1705→        return (
  1706→  1706→          <Button disabled className={`${btnClass} cursor-default`} style={{
  1707→  1707→            backgroundColor: isDetecting ? '#22c55e33' : THEME.panel,
  1708→  1708→            color: isDetecting ? '#4ade80' : THEME.muted,
  1709→  1709→            border: `2px solid ${isDetecting ? '#22c55e' : THEME.border}`,
  1710→  1710→          }}>
  1711→  1711→            {isDetecting ? <Loader2 className={`${iconCls} mr-2 animate-spin`} /> : <Aperture className={`${iconCls} mr-2`} />}
  1712→  1712→            {isDetecting ? 'AI Mendeteksi Pose...' : 'AI Loading...'}
  1713→  1713→          </Button>
  1714→  1714→        )
  1715→  1715→      }
  1716→  1716→
  1717→  1717→      if (isTimer) {
  1718→  1718→        return (
  1719→  1719→          <Button onClick={handleCaptureButtonClick} className={btnClass} style={{
  1720→  1720→            backgroundColor: THEME.gold,
  1721→  1721→            color: THEME.bg,
  1722→  1722→            border: `2px solid ${THEME.gold}`,
  1723→  1723→            boxShadow: `0 0 30px ${THEME.gold}44, 0 0 60px ${THEME.gold}22`,
  1724→  1724→          }}>
  1725→  1725→            <Timer className={`${iconCls} mr-2`} />
  1726→  1726→            {photoshoot ? `FOTO (${getTimerDuration(effectiveShutterMode)}s)` : `FOTO 1 — TOGA (${getTimerDuration(effectiveShutterMode)}s)`}
  1727→  1727→          </Button>
  1728→  1728→        )
  1729→  1729→      }
  1730→  1730→
  1731→  1731→      // Manual mode
  1732→  1732→      return (
  1733→  1733→        <Button onClick={handleCaptureButtonClick} className={btnClass} style={photoshoot
  1734→  1734→          ? { backgroundColor: '#4ade80', color: '#1a0b2e', border: '2px solid #4ade80', boxShadow: '0 0 30px #4ade8044, 0 0 60px #4ade8022' }
  1735→  1735→          : { backgroundColor: THEME.gold, color: THEME.bg, border: `2px solid ${THEME.gold}`, boxShadow: `0 0 30px ${THEME.gold}44, 0 0 60px ${THEME.gold}22` }
  1736→  1736→        }>
  1737→  1737→          <Camera className={`${iconCls} mr-2`} />{photoshoot ? 'FOTO' : 'FOTO 1 — TOGA'}
  1738→  1738→        </Button>
  1739→  1739→      )
  1740→  1740→    }
  1741→  1741→
  1742→  1742→    if (capturePhase === 'ready-2') {
  1743→  1743→      const isAutoMode = effectiveShutterMode === 'ai'
  1744→  1744→      const isTimer = isTimerMode(effectiveShutterMode)
  1745→  1745→
  1746→  1746→      if (isAutoMode) {
  1747→  1747→        const isDetecting = ai.isRunning
  1748→  1748→        return (
  1749→  1749→          <Button disabled className={`${btnClass} cursor-default`} style={{
  1750→  1750→            backgroundColor: isDetecting ? '#22c55e33' : THEME.panel,
  1751→  1751→            color: isDetecting ? '#4ade80' : THEME.muted,
  1752→  1752→            border: `2px solid ${isDetecting ? '#22c55e' : THEME.border}`,
  1753→  1753→          }}>
  1754→  1754→            {isDetecting ? <Loader2 className={`${iconCls} mr-2 animate-spin`} /> : <Aperture className={`${iconCls} mr-2`} />}
  1755→  1755→            AI Mendeteksi Ijazah...
  1756→  1756→          </Button>
  1757→  1757→        )
  1758→  1758→      }
  1759→  1759→
  1760→  1760→      if (isTimer) {
  1761→  1761→        return (
  1762→  1762→          <Button onClick={handleCaptureButtonClick} className={btnClass} style={{
  1763→  1763→            backgroundColor: THEME.gold,
  1764→  1764→            color: THEME.bg,
  1765→  1765→            border: `2px solid ${THEME.gold}`,
  1766→  1766→            boxShadow: `0 0 30px ${THEME.gold}44, 0 0 60px ${THEME.gold}22`,
  1767→  1767→          }}>
  1768→  1768→            <Timer className={`${iconCls} mr-2`} />
  1769→  1769→            FOTO 2 — IJAZAH ({getTimerDuration(effectiveShutterMode)}s)
  1770→  1770→          </Button>
  1771→  1771→        )
  1772→  1772→      }
  1773→  1773→
  1774→  1774→      return (
  1775→  1775→        <Button onClick={handleCaptureButtonClick} className={btnClass} style={{ backgroundColor: '#22c55e', color: '#ffffff', border: '2px solid #22c55e', boxShadow: '0 0 30px #22c55e44, 0 0 60px #22c55e22' }}>
  1776→  1776→          <Camera className={`${iconCls} mr-2`} />FOTO 2 — IJAZAH
  1777→  1777→        </Button>
  1778→  1778→      )
  1779→  1779→    }
  1780→  1780→
  1781→  1781→    if (capturePhase === 'sending') {
  1782→  1782→      return (
  1783→  1783→        <Button disabled className={`${btnClass} cursor-not-allowed`} style={{ backgroundColor: THEME.panel, color: THEME.muted, border: `2px solid ${THEME.border}` }}>
  1784→  1784→          <Loader2 className={`${iconCls} mr-2 animate-spin`} />MENGIRIM...
  1785→  1785→        </Button>
  1786→  1786→      )
  1787→  1787→    }
  1788→  1788→
  1789→  1789→    return null
  1790→  1790→  }
  1791→  1791→
  1792→  1792→  // ── Queue list (shared) ──────────────────────────────────────────────────
  1793→  1793→  const renderQueueList = (compact = false) => (
  1794→  1794→    <Card
  1795→  1795→      className={`${compact ? 'flex-1 min-h-0' : 'flex-1 min-h-0'} border rounded-lg overflow-hidden flex flex-col`}
  1796→  1796→      style={{ backgroundColor: THEME.card, borderColor: THEME.border }}
  1797→  1797→    >
  1798→  1798→      <div
  1799→  1799→        className="shrink-0 flex items-center justify-between px-3 py-2"
  1800→  1800→        style={{ borderBottom: `1px solid ${THEME.border}` }}
  1801→  1801→      >
  1802→  1802→        <h3 className="text-xs font-semibold" style={{ color: '#ffffff' }}>
  1803→  1803→          Antrean: <span style={{ color: THEME.gold }} className="font-bold">{remainingCount}</span>
  1804→  1804→        </h3>
  1805→  1805→        <span className="text-[10px]" style={{ color: THEME.muted }}>Ch.{myChannel}</span>
  1806→  1806→      </div>
  1807→  1807→
  1808→  1808→      {!compact && (
  1809→  1809→        <div
  1810→  1810→          className="shrink-0 grid grid-cols-[24px_60px_1fr_60px] gap-0.5 px-2 py-1 text-[8px] font-semibold uppercase tracking-wider"
  1811→  1811→          style={{ backgroundColor: THEME.panel, color: THEME.muted, borderBottom: `1px solid ${THEME.border}` }}
  1812→  1812→        >
  1813→  1813→          <span>No</span><span>NIM</span><span>Nama</span><span className="text-right">Status</span>
  1814→  1814→        </div>
  1815→  1815→      )}
  1816→  1816→
  1817→  1817→      <ScrollArea className="flex-1 min-h-0">
  1818→  1818→        <div className="flex flex-col">
  1819→  1819→          {channelStudents.length === 0 ? (
  1820→  1820→            <div className="flex items-center justify-center py-8">
  1821→  1821→              <p className="text-xs" style={{ color: THEME.muted }}>Tidak ada mahasiswa</p>
  1822→  1822→            </div>
  1823→  1823→          ) : (
  1824→  1824→            channelStudents.map((student, idx) => {
  1825→  1825→              const isActive = student.status === `active_${myChannel}`
  1826→  1826→              const isNext = student.id === nextPending?.id && student.status === 'pending'
  1827→  1827→
  1828→  1828→              if (compact) {
  1829→  1829→                return (
  1830→  1830→                  <div
  1831→  1831→                    key={student.id}
  1832→  1832→                    ref={isActive ? activeRowRef : isNext ? nextRowRef : undefined}
  1833→  1833→                    className="flex items-center gap-2 px-3 py-1.5 transition-colors duration-200"
  1834→  1834→                    style={getRowStyle(student)}
  1835→  1835→                  >
  1836→  1836→                    <span className="text-[10px] font-mono w-5 shrink-0" style={{ color: THEME.muted }}>{idx + 1}</span>
  1837→  1837→                    <span className={`text-xs font-medium truncate flex-1 ${student.status === 'done' ? 'line-through' : ''}`} style={{ color: isActive ? THEME.gold : student.status === 'done' ? THEME.muted : '#ffffff' }}>
  1838→  1838→                      {student.nama}
  1839→  1839→                    </span>
  1840→  1840→                    <div className="shrink-0">{renderStatusBadge(student.status)}</div>
  1841→  1841→                  </div>
  1842→  1842→                )
  1843→  1843→              }
  1844→  1844→
  1845→  1845→              return (
  1846→  1846→                <div
  1847→  1847→                  key={student.id}
  1848→  1848→                  ref={isActive ? activeRowRef : isNext ? nextRowRef : undefined}
  1849→  1849→                  className="grid grid-cols-[24px_60px_1fr_60px] gap-0.5 items-center px-2 py-1 transition-colors duration-200"
  1850→  1850→                  style={getRowStyle(student)}
  1851→  1851→                >
  1852→  1852→                  <span className="text-[9px] font-mono" style={{ color: THEME.muted }}>{idx + 1}</span>
  1853→  1853→                  <span className="text-[9px] font-mono truncate" style={{ color: THEME.muted }}>{student.nim}</span>
  1854→  1854→                  <span className={`text-[10px] font-medium truncate ${student.status === 'done' ? 'line-through' : ''}`} style={{ color: isActive ? THEME.gold : student.status === 'done' ? THEME.muted : '#ffffff' }}>
  1855→  1855→                    {student.nama}
  1856→  1856→                  </span>
  1857→  1857→                  <div className="flex justify-end">{renderStatusBadge(student.status)}</div>
  1858→  1858→                </div>
  1859→  1859→              )
  1860→  1860→            })
  1861→  1861→          )}
  1862→  1862→        </div>
  1863→  1863→      </ScrollArea>
  1864→  1864→    </Card>
  1865→  1865→  )
  1866→  1866→
  1867→  1867→  // ── Main render ──────────────────────────────────────────────────────────
  1868→  1868→  if (!currentProject) {
  1869→  1869→    return (
  1870→  1870→      <div className="flex items-center justify-center h-full" style={{ backgroundColor: THEME.bg, color: THEME.muted }}>
  1871→  1871→        <p className="text-sm opacity-60">Belum ada proyek aktif</p>
  1872→  1872→      </div>
  1873→  1873→    )
  1874→  1874→  }
  1875→  1875→
  1876→  1876→  // ── MOBILE LAYOUT ────────────────────────────────────────────────────────
  1877→  1877→  if (isMobile) {
  1878→  1878→    return (
  1879→  1879→      <div className="flex flex-col h-full touch-no-select" style={{ backgroundColor: THEME.bg }}>
  1880→  1880→
  1881→  1881→        {/* ── Camera Zone ────────────────────────────────────────────────── */}
  1882→  1882→        <div
  1883→  1883→          ref={cameraZoneRef}
  1884→  1884→          className="flex-1 flex items-center justify-center min-h-0 p-1"
  1885→  1885→        >
  1886→  1886→          {renderCameraView()}
  1887→  1887→        </div>
  1888→  1888→
  1889→  1889→        {/* ── Capture Button (centered, directly below camera view) ─────── */}
  1890→  1890→        <div
  1891→  1891→          className="shrink-0 w-full px-3 py-2 border-t"
  1892→  1892→          style={{
  1893→  1893→            backgroundColor: `${THEME.panel}ee`,
  1894→  1894→            borderColor: THEME.border,
  1895→  1895→            backdropFilter: 'blur(12px)',
  1896→  1896→          }}
  1897→  1897→        >
  1898→  1898→          <div className="mx-auto w-full max-w-md">
  1899→  1899→            {renderCaptureButton('xl')}
  1900→  1900→          </div>
  1901→  1901→        </div>
  1902→  1902→
  1903→  1903→        {/* ── Bottom Control Bar ──────────────────────────────────────────── */}
  1904→  1904→        <div
  1905→  1905→          className="shrink-0 border-t safe-bottom"
  1906→  1906→          style={{
  1907→  1907→            backgroundColor: `${THEME.panel}ee`,
  1908→  1908→            borderColor: THEME.border,
  1909→  1909→            backdropFilter: 'blur(12px)',
  1910→  1910→          }}
  1911→  1911→        >
  1912→  1912→          {/* Target info row */}
  1913→  1913→          <div className="flex items-center gap-2 px-3 py-2">
  1914→  1914→            <div
  1915→  1915→              className="shrink-0 flex items-center justify-center w-8 h-8 rounded-full border-2"
  1916→  1916→              style={{
  1917→  1917→                backgroundColor: THEME.bg,
  1918→  1918→                borderColor: hasActiveTarget ? THEME.gold : THEME.border,
  1919→  1919→              }}
  1920→  1920→            >
  1921→  1921→              <User className="size-3.5" style={{ color: hasActiveTarget ? THEME.gold : THEME.border }} />
  1922→  1922→            </div>
  1923→  1923→
  1924→  1924→            <div className="flex-1 min-w-0">
  1925→  1925→              {hasActiveTarget ? (
  1926→  1926→                <div className="flex items-center gap-2">
  1927→  1927→                  <div className="min-w-0">
  1928→  1928→                    <p className="text-sm font-bold leading-tight truncate" style={{ color: '#ffffff' }}>
  1929→  1929→                      {opCurrentTarget.nama}
  1930→  1930→                    </p>
  1931→  1931→                    <p className="text-[10px] font-mono" style={{ color: THEME.muted }}>
  1932→  1932→                      {opCurrentTarget.nim}
  1933→  1933→                    </p>
  1934→  1934→                  </div>
  1935→  1935→                  <Badge
  1936→  1936→                    className={`text-[9px] px-1.5 py-0.5 shrink-0 ${hasActiveTarget && !sending ? 'animate-pulse' : ''}`}
  1937→  1937→                    style={{
  1938→  1938→                      backgroundColor:
  1939→  1939→                        capturePhase === 'ready-1' ? `${THEME.gold}33`
  1940→  1940→                          : capturePhase === 'ready-2' ? '#22c55e33'
  1941→  1941→                            : capturePhase === 'sending' ? `${THEME.border}66`
  1942→  1942→                              : `${THEME.border}44`,
  1943→  1943→                      color:
  1944→  1944→                        capturePhase === 'ready-1' ? THEME.gold
  1945→  1945→                          : capturePhase === 'ready-2' ? '#4ade80'
  1946→  1946→                            : THEME.muted,
  1947→  1947→                      border: `1px solid ${capturePhase === 'ready-1' ? `${THEME.gold}66` : capturePhase === 'ready-2' ? '#22c55e66' : THEME.border}`,
  1948→  1948→                    }}
  1949→  1949→                  >
  1950→  1950→                    {capturePhase === 'sending' && <Loader2 className="size-2.5 mr-0.5 animate-spin" />}
  1951→  1951→                    {capturePhase === 'ready-1' && <Camera className="size-2.5 mr-0.5" />}
  1952→  1952→                    {capturePhase === 'ready-2' && <CheckCircle2 className="size-2.5 mr-0.5" />}
  1953→  1953→                    {capturePhase === 'standby' && <Clock className="size-2.5 mr-0.5" />}
  1954→  1954→                    {progressText}
  1955→  1955→                  </Badge>
  1956→  1956→                </div>
  1957→  1957→              ) : (
  1958→  1958→                <p className="text-xs italic" style={{ color: THEME.muted }}>
  1959→  1959→                  Menunggu panggilan MC...
  1960→  1960→                </p>
  1961→  1961→              )}
  1962→  1962→            </div>
  1963→  1963→
  1964→  1964→            <button
  1965→  1965→              onClick={() => setShowQueueOnMobile(!showQueueOnMobile)}
  1966→  1966→              className="shrink-0 flex items-center justify-center w-9 h-9 rounded-lg border cursor-pointer active:scale-95 transition-transform"
  1967→  1967→              style={{
  1968→  1968→                backgroundColor: showQueueOnMobile ? THEME.gold : THEME.card,
  1969→  1969→                borderColor: showQueueOnMobile ? THEME.gold : THEME.border,
  1970→  1970→              }}
  1971→  1971→              title="Lihat antrean"
  1972→  1972→            >
  1973→  1973→              {showQueueOnMobile ? (
  1974→  1974→                <X className="size-4" style={{ color: showQueueOnMobile ? THEME.bg : THEME.muted }} />
  1975→  1975→              ) : (
  1976→  1976→                <List className="size-4" style={{ color: THEME.muted }} />
  1977→  1977→              )}
  1978→  1978→            </button>
  1979→  1979→          </div>
  1980→  1980→
  1981→  1981→          {/* Operator search (photoshoot only) */}
  1982→  1982→          {showQueueOnMobile && renderOpSearch(true)}
  1983→  1983→
  1984→  1984→          {/* Queue list (expandable) */}
  1985→  1985→          {showQueueOnMobile && (
  1986→  1986→            <div
  1987→  1987→              className="border-t"
  1988→  1988→              style={{ borderColor: THEME.border, maxHeight: '35vh' }}
  1989→  1989→            >
  1990→  1990→              {renderQueueList(true)}
  1991→  1991→            </div>
  1992→  1992→          )}
  1993→  1993→
  1994→  1994→          {/* Camera selector (compact) + Shutter mode selector */}
  1995→  1995→          <div className="px-3 py-1 flex items-center gap-2">
  1996→  1996→            {videoDevices.length > 1 && (
  1997→  1997→              <Select value={selectedDeviceId} onValueChange={setSelectedDeviceId}>
  1998→  1998→                <SelectTrigger
  1999→  1999→                  className="text-[10px] h-7 w-32"
  2000→  2000→                  style={{ backgroundColor: THEME.bg, borderColor: THEME.border, color: THEME.muted }}

[showing lines 1-2000 of 2002; use offset=2001 with limit to continue reading]